apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-alb-stack-card
  namespace: aws-alb-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-alb-stack-card
  namespace: aws-alb-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-alb-stack-customform
      namespace: aws-alb-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-alb-stack
      namespace: aws-alb-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Application/Network Load Balancer (LoadBalancer, target groups, listeners) — inputs mirror terraform-aws-modules/alb/aws.
      icon: fa-brands fa-aws
      tags: aws-alb-system,0.3.0
      title: aws-alb-stack
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-alb-stack-card
    namespace: aws-alb-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-alb-system/compositiondefinitions/aws-alb-stack"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-alb-stack-customform
  namespace: aws-alb-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-alb-stack-customform
  namespace: aws-alb-system
spec:
  type: customform
  propsRef:
    name: template-aws-alb-stack-customform
    namespace: aws-alb-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-3-0
        verb: POST
        resource: awsalbstacks
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-alb-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-3-0") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsalbstacks.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
