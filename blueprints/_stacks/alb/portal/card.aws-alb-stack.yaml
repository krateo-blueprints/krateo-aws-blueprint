# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-alb-stack
  namespace: aws-alb-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Networking
spec:
  apiRef:
    name: aws-alb-stack-card-data
    namespace: aws-alb-system
  widgetData:
    title: Aws Alb Stack
    headerLeft: Provision an AWS Application/Network Load Balancer (LoadBalancer, target groups, listeners) — inputs mirror terraform-aws-modules/alb/aws.
    tooltip: Aws Alb Stack blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Networking
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-alb-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Networking", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-alb-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-alb-stack-install
        namespace: aws-alb-system
        resource: buttons
        verb: GET
