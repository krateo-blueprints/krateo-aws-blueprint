# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-autoscaling-stack
  namespace: aws-autoscaling-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-autoscaling-stack-card-data
    namespace: aws-autoscaling-system
  widgetData:
    title: Aws Autoscaling Stack
    headerLeft: Provision an AWS EC2 Auto Scaling group (launch template + auto scaling group) — inputs mirror terraform-aws-modules/autoscaling/aws.
    tooltip: Aws Autoscaling Stack blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-autoscaling-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-autoscaling-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-autoscaling-stack-install
        namespace: aws-autoscaling-system
        resource: buttons
        verb: GET
