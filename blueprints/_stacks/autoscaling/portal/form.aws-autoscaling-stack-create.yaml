# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Form
metadata:
  name: aws-autoscaling-stack-create
  namespace: aws-autoscaling-system
spec:
  apiRef:
    name: aws-autoscaling-stack-formdef
    namespace: aws-autoscaling-system
  widgetData:
    layout: vertical
    submitActionId: submit
    propertiesToHide: []
    schema: {}
    buttonConfig:
      primary:
        label: Review & create
      secondary:
        label: Cancel
    actions:
      rest:
        - id: submit
          type: rest
          resourceRefId: create-composition
          headers:
            - 'Content-Type: application/json'
          onSuccessNavigateTo: /compositions
          successMessage: Aws Autoscaling Stack composition created
          payloadToOverride:
            - name: spec
              value: ${ .json }
  widgetDataTemplate:
    - forPath: schema
      expression: ${ .schemaSpec }
  resourcesRefs:
    items:
      - id: create-composition
        apiVersion: composition.krateo.io/v0-3-0
        resource: awsautoscalingstacks
        namespace: aws-autoscaling-system
        verb: POST
        payload:
          apiVersion: composition.krateo.io/v0-3-0
          kind: Awsautoscalingstack
          metadata:
            namespace: aws-autoscaling-system
