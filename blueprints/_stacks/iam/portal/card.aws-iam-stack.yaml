# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-iam-stack
  namespace: aws-iam-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-iam-stack-card-data
    namespace: aws-iam-system
  widgetData:
    title: Aws Iam Stack
    headerLeft: Provision an AWS IAM role (with optional inline policy and attached managed policies) — inputs mirror terraform-aws-modules/iam/aws.
    tooltip: Aws Iam Stack blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-iam-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-iam-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-iam-stack-install
        namespace: aws-iam-system
        resource: buttons
        verb: GET
