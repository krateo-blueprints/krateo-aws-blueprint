# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-stack
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-rds-stack-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Stack
    headerLeft: Provision an AWS RDS database (DB subnet group, DB instance, optional parameter group) — inputs mirror terraform-aws-modules/rds/aws.
    tooltip: Aws Rds Stack blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-stack-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
