apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-security-group-stack-card
  namespace: aws-security-group-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-security-group-stack-card
  namespace: aws-security-group-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-security-group-stack-customform
      namespace: aws-security-group-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-security-group-stack
      namespace: aws-security-group-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS EC2 Security Group with ingress/egress rules — inputs mirror terraform-aws-modules/security-group/aws.
      icon: fa-solid fa-shield-halved
      tags: aws-security-group-system,0.3.0
      title: aws-security-group-stack
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-security-group-stack-card
    namespace: aws-security-group-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-security-group-system/compositiondefinitions/aws-security-group-stack"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-security-group-stack-customform
  namespace: aws-security-group-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-security-group-stack-customform
  namespace: aws-security-group-system
spec:
  type: customform
  propsRef:
    name: template-aws-security-group-stack-customform
    namespace: aws-security-group-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-3-0
        verb: POST
        resource: awssecuritygroupstacks
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-security-group-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-3-0") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awssecuritygroupstacks.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
