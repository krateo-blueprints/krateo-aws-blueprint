# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-security-group-stack
  namespace: aws-security-group-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-security-group-stack-card-data
    namespace: aws-security-group-system
  widgetData:
    title: Aws Security Group Stack
    headerLeft: Provision an AWS EC2 Security Group with ingress/egress rules — inputs mirror terraform-aws-modules/security-group/aws.
    tooltip: Aws Security Group Stack blueprint
    icon:
      name: fa-shield-halved
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-security-group-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-shield-halved", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-security-group-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-security-group-stack-install
        namespace: aws-security-group-system
        resource: buttons
        verb: GET
