# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-vpc-stack
  namespace: aws-vpc-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Networking
spec:
  apiRef:
    name: aws-vpc-stack-card-data
    namespace: aws-vpc-system
  widgetData:
    title: Aws Vpc Stack
    headerLeft: Provision an AWS VPC (subnets, route tables, IGW, NAT) — inputs mirror terraform-aws-modules/vpc/aws.
    tooltip: Aws Vpc Stack blueprint
    icon:
      name: fa-network-wired
      color: grey
    tags:
      - Networking
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-vpc-stack-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Networking", .version] }
    - forPath: icon
      expression: '${ { name: "fa-network-wired", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-vpc-stack-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-vpc-stack-install
        namespace: aws-vpc-system
        resource: buttons
        verb: GET
