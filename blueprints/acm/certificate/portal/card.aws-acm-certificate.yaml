# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-acm-certificate
  namespace: aws-acm-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-acm-certificate-card-data
    namespace: aws-acm-system
  widgetData:
    title: Aws Acm Certificate
    headerLeft: Provision an AWS Acm Certificate via the ACK acm controller.
    tooltip: Aws Acm Certificate blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-acm-certificate-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-acm-certificate-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-acm-certificate-install
        namespace: aws-acm-system
        resource: buttons
        verb: GET
