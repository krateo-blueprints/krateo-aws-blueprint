# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-apigateway-apimethodresponse
  namespace: aws-apigateway-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-apigateway-apimethodresponse-card-data
    namespace: aws-apigateway-system
  widgetData:
    title: Aws Apigateway Apimethodresponse
    headerLeft: Provision an AWS Apigateway APIMethodResponse via the ACK apigateway controller.
    tooltip: Aws Apigateway Apimethodresponse blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-apigateway-apimethodresponse-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-apigateway-apimethodresponse-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-apigateway-apimethodresponse-install
        namespace: aws-apigateway-system
        resource: buttons
        verb: GET
