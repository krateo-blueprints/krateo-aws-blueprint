# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-apigateway-integration
  namespace: aws-apigateway-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-apigateway-integration-card-data
    namespace: aws-apigateway-system
  widgetData:
    title: Aws Apigateway Integration
    headerLeft: Provision an AWS Apigateway Integration via the ACK apigateway controller.
    tooltip: Aws Apigateway Integration blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-apigateway-integration-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-apigateway-integration-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-apigateway-integration-install
        namespace: aws-apigateway-system
        resource: buttons
        verb: GET
