# Krateo Blueprint — AWS Apigateway Method

> Generated by `tools/ackgen` from the ACK `Method` CRD (`apigateway.services.k8s.aws`).

Provisions an AWS Apigateway **Method** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
apigateway controller. The chart renders a native `apigateway.services.k8s.aws/v1alpha1` `Method`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsApigatewayMethod` Composition you create
becomes an AWS Method.

## Prerequisites

- **ACK apigateway controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-apigateway-controller \
    oci://public.ecr.aws/aws-controllers-k8s/apigateway-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK Method spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-apigateway-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsApigatewayMethod type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsApigatewayMethod` Composition type (`composition.krateo.io/v0-1-1`,
plural `awsapigatewaymethods`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-apigateway-method`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsApigatewayMethod
metadata:
  name: my-method
  namespace: aws-apigateway-system
spec:
  region: eu-west-1
  # ... ACK Method spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsapigatewaymethods.composition.krateo.io -n aws-apigateway-system
kubectl get methods.apigateway.services.k8s.aws -A
```
