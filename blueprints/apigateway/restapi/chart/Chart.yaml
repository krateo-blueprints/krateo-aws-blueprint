apiVersion: v2
# Generated by tools/ackgen from the ACK RestAPI CRD (apigateway.services.k8s.aws).
#
# Renders a native apigateway.services.k8s.aws/v1alpha1 RestAPI resource; the ACK apigateway
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsApigatewayRestapi (plural awsapigatewayrestapis) in the composition.krateo.io group.
# The Composition spec mirrors the ACK RestAPI spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-apigateway-restapi
description: A Krateo blueprint that provisions an AWS Apigateway RestAPI via the AWS Controllers for Kubernetes (ACK) apigateway controller. Requires the ACK apigateway controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - apigateway
  - restapi
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "network"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
