# Krateo Blueprint — AWS Apigatewayv2 API

> Generated by `tools/ackgen` from the ACK `API` CRD (`apigatewayv2.services.k8s.aws`).

Provisions an AWS Apigatewayv2 **API** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
apigatewayv2 controller. The chart renders a native `apigatewayv2.services.k8s.aws/v1alpha1` `API`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsApigatewayv2API` Composition you create
becomes an AWS API.

## Prerequisites

- **ACK apigatewayv2 controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-apigatewayv2-controller \
    oci://public.ecr.aws/aws-controllers-k8s/apigatewayv2-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK API spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-apigatewayv2-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsApigatewayv2API type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsApigatewayv2API` Composition type (`composition.krateo.io/v0-1-0`,
plural `awsapigatewayv2apis`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-apigatewayv2-api`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsApigatewayv2API
metadata:
  name: my-api
  namespace: aws-apigatewayv2-system
spec:
  region: eu-west-1
  # ... ACK API spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsapigatewayv2apis.composition.krateo.io -n aws-apigatewayv2-system
kubectl get apis.apigatewayv2.services.k8s.aws -A
```
