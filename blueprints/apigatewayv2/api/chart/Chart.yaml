apiVersion: v2
# Generated by tools/ackgen from the ACK API CRD (apigatewayv2.services.k8s.aws).
#
# Renders a native apigatewayv2.services.k8s.aws/v1alpha1 API resource; the ACK apigatewayv2
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsApigatewayv2API (plural awsapigatewayv2apis) in the composition.krateo.io group.
# The Composition spec mirrors the ACK API spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-apigatewayv2-api
description: A Krateo blueprint that provisions an AWS Apigatewayv2 API via the AWS Controllers for Kubernetes (ACK) apigatewayv2 controller. Requires the ACK apigatewayv2 controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - apigatewayv2
  - api
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
