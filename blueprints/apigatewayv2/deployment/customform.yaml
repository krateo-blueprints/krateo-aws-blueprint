apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-apigatewayv2-deployment-card
  namespace: aws-apigatewayv2-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-apigatewayv2-deployment-card
  namespace: aws-apigatewayv2-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-apigatewayv2-deployment-customform
      namespace: aws-apigatewayv2-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-apigatewayv2-deployment
      namespace: aws-apigatewayv2-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Apigatewayv2 Deployment via the ACK apigatewayv2 controller.
      icon: fa-brands fa-aws
      tags: aws-apigatewayv2-system,0.1.1
      title: aws-apigatewayv2-deployment
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-apigatewayv2-deployment-card
    namespace: aws-apigatewayv2-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-apigatewayv2-system/compositiondefinitions/aws-apigatewayv2-deployment"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-apigatewayv2-deployment-customform
  namespace: aws-apigatewayv2-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-apigatewayv2-deployment-customform
  namespace: aws-apigatewayv2-system
spec:
  type: customform
  propsRef:
    name: template-aws-apigatewayv2-deployment-customform
    namespace: aws-apigatewayv2-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-1
        verb: POST
        resource: awsapigatewayv2deployments
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-apigatewayv2-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-1") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsapigatewayv2deployments.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
