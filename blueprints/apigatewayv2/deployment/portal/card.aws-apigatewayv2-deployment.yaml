# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-apigatewayv2-deployment
  namespace: aws-apigatewayv2-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-apigatewayv2-deployment-card-data
    namespace: aws-apigatewayv2-system
  widgetData:
    title: Aws Apigatewayv2 Deployment
    headerLeft: Provision an AWS Apigatewayv2 Deployment via the ACK apigatewayv2 controller.
    tooltip: Aws Apigatewayv2 Deployment blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-apigatewayv2-deployment-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-apigatewayv2-deployment-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-apigatewayv2-deployment-install
        namespace: aws-apigatewayv2-system
        resource: buttons
        verb: GET
