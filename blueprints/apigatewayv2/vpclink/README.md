# Krateo Blueprint — AWS Apigatewayv2 VPCLink

> Generated by `tools/ackgen` from the ACK `VPCLink` CRD (`apigatewayv2.services.k8s.aws`).

Provisions an AWS Apigatewayv2 **VPCLink** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
apigatewayv2 controller. The chart renders a native `apigatewayv2.services.k8s.aws/v1alpha1` `VPCLink`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsApigatewayv2Vpclink` Composition you create
becomes an AWS VPCLink.

## Prerequisites

- **ACK apigatewayv2 controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-apigatewayv2-controller \
    oci://public.ecr.aws/aws-controllers-k8s/apigatewayv2-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK VPCLink spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-apigatewayv2-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsApigatewayv2Vpclink type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsApigatewayv2Vpclink` Composition type (`composition.krateo.io/v0-1-0`,
plural `awsapigatewayv2vpclinks`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-apigatewayv2-vpclink`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsApigatewayv2Vpclink
metadata:
  name: my-vpclink
  namespace: aws-apigatewayv2-system
spec:
  region: eu-west-1
  # ... ACK VPCLink spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsapigatewayv2vpclinks.composition.krateo.io -n aws-apigatewayv2-system
kubectl get vpclinks.apigatewayv2.services.k8s.aws -A
```
