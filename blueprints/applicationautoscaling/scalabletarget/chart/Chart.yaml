apiVersion: v2
# Generated by tools/ackgen from the ACK ScalableTarget CRD (applicationautoscaling.services.k8s.aws).
#
# Renders a native applicationautoscaling.services.k8s.aws/v1alpha1 ScalableTarget resource; the ACK applicationautoscaling
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsApplicationautoscalingScalabletarget (plural awsapplicationautoscalingscalabletargets) in the composition.krateo.io group.
# The Composition spec mirrors the ACK ScalableTarget spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-applicationautoscaling-scalabletarget
description: A Krateo blueprint that provisions an AWS Applicationautoscaling ScalableTarget via the AWS Controllers for Kubernetes (ACK) applicationautoscaling controller. Requires the ACK applicationautoscaling controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - applicationautoscaling
  - scalabletarget
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
