# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-applicationautoscaling-scalabletarget
  namespace: aws-applicationautoscaling-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-applicationautoscaling-scalabletarget-card-data
    namespace: aws-applicationautoscaling-system
  widgetData:
    title: Aws Applicationautoscaling Scalabletarget
    headerLeft: Provision an AWS Applicationautoscaling ScalableTarget via the ACK applicationautoscaling controller.
    tooltip: Aws Applicationautoscaling Scalabletarget blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-applicationautoscaling-scalabletarget-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-applicationautoscaling-scalabletarget-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-applicationautoscaling-scalabletarget-install
        namespace: aws-applicationautoscaling-system
        resource: buttons
        verb: GET
