apiVersion: core.krateo.io/v1alpha1
kind: CompositionDefinition
metadata:
  name: aws-applicationautoscaling-scalingpolicy
  namespace: aws-applicationautoscaling-system
spec:
  chart:
    # OCI artifact published by .github/workflows/release-tag.yaml.
    url: oci://ghcr.io/krateo-blueprints/charts/aws-applicationautoscaling-scalingpolicy
    version: "0.1.1"
    # If the GHCR package is private, provide pull credentials:
    # credentials:
    #   username: braghettos
    #   passwordRef:
    #     key: token
    #     name: ghcr-credentials
    #     namespace: aws-applicationautoscaling-system
