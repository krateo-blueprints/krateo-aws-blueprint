# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-applicationautoscaling-scalingpolicy
  namespace: aws-applicationautoscaling-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-applicationautoscaling-scalingpolicy-card-data
    namespace: aws-applicationautoscaling-system
  widgetData:
    title: Aws Applicationautoscaling Scalingpolicy
    headerLeft: Provision an AWS Applicationautoscaling ScalingPolicy via the ACK applicationautoscaling controller.
    tooltip: Aws Applicationautoscaling Scalingpolicy blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-applicationautoscaling-scalingpolicy-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-applicationautoscaling-scalingpolicy-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-applicationautoscaling-scalingpolicy-install
        namespace: aws-applicationautoscaling-system
        resource: buttons
        verb: GET
