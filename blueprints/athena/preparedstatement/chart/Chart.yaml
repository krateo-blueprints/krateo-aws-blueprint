apiVersion: v2
# Generated by tools/ackgen from the ACK PreparedStatement CRD (athena.services.k8s.aws).
#
# Renders a native athena.services.k8s.aws/v1alpha1 PreparedStatement resource; the ACK athena
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsAthenaPreparedstatement (plural awsathenapreparedstatements) in the composition.krateo.io group.
# The Composition spec mirrors the ACK PreparedStatement spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-athena-preparedstatement
description: A Krateo blueprint that provisions an AWS Athena PreparedStatement via the AWS Controllers for Kubernetes (ACK) athena controller. Requires the ACK athena controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - athena
  - preparedstatement
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
