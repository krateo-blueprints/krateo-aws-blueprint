# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-athena-preparedstatement
  namespace: aws-athena-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-athena-preparedstatement-card-data
    namespace: aws-athena-system
  widgetData:
    title: Aws Athena Preparedstatement
    headerLeft: Provision an AWS Athena PreparedStatement via the ACK athena controller.
    tooltip: Aws Athena Preparedstatement blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-athena-preparedstatement-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-athena-preparedstatement-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-athena-preparedstatement-install
        namespace: aws-athena-system
        resource: buttons
        verb: GET
