# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-athena-workgroup
  namespace: aws-athena-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-athena-workgroup-card-data
    namespace: aws-athena-system
  widgetData:
    title: Aws Athena Workgroup
    headerLeft: Provision an AWS Athena WorkGroup via the ACK athena controller.
    tooltip: Aws Athena Workgroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-athena-workgroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-athena-workgroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-athena-workgroup-install
        namespace: aws-athena-system
        resource: buttons
        verb: GET
