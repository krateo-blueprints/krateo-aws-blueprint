apiVersion: v2
# Generated by tools/ackgen from the ACK AutoScalingGroup CRD (autoscaling.services.k8s.aws).
#
# Renders a native autoscaling.services.k8s.aws/v1alpha1 AutoScalingGroup resource; the ACK autoscaling
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsAutoscalingAutoscalinggroup (plural awsautoscalingautoscalinggroups) in the composition.krateo.io group.
# The Composition spec mirrors the ACK AutoScalingGroup spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-autoscaling-autoscalinggroup
description: A Krateo blueprint that provisions an AWS Autoscaling AutoScalingGroup via the AWS Controllers for Kubernetes (ACK) autoscaling controller. Requires the ACK autoscaling controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - autoscaling
  - autoscalinggroup
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
