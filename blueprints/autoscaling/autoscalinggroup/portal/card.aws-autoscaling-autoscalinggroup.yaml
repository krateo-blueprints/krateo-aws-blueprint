# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-autoscaling-autoscalinggroup
  namespace: aws-autoscaling-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-autoscaling-autoscalinggroup-card-data
    namespace: aws-autoscaling-system
  widgetData:
    title: Aws Autoscaling Autoscalinggroup
    headerLeft: Provision an AWS Autoscaling AutoScalingGroup via the ACK autoscaling controller.
    tooltip: Aws Autoscaling Autoscalinggroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-autoscaling-autoscalinggroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-autoscaling-autoscalinggroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-autoscaling-autoscalinggroup-install
        namespace: aws-autoscaling-system
        resource: buttons
        verb: GET
