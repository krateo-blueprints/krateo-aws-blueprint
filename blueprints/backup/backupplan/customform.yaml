apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-backup-backupplan-card
  namespace: aws-backup-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-backup-backupplan-card
  namespace: aws-backup-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-backup-backupplan-customform
      namespace: aws-backup-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-backup-backupplan
      namespace: aws-backup-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Backup BackupPlan via the ACK backup controller.
      icon: fa-brands fa-aws
      tags: aws-backup-system,0.1.1
      title: aws-backup-backupplan
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-backup-backupplan-card
    namespace: aws-backup-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-backup-system/compositiondefinitions/aws-backup-backupplan"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-backup-backupplan-customform
  namespace: aws-backup-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-backup-backupplan-customform
  namespace: aws-backup-system
spec:
  type: customform
  propsRef:
    name: template-aws-backup-backupplan-customform
    namespace: aws-backup-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-1
        verb: POST
        resource: awsbackupbackupplans
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-backup-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-1") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsbackupbackupplans.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
