# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-backup-backupplan
  namespace: aws-backup-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Storage
spec:
  apiRef:
    name: aws-backup-backupplan-card-data
    namespace: aws-backup-system
  widgetData:
    title: Aws Backup Backupplan
    headerLeft: Provision an AWS Backup BackupPlan via the ACK backup controller.
    tooltip: Aws Backup Backupplan blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Storage
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-backup-backupplan-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Storage", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-backup-backupplan-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-backup-backupplan-install
        namespace: aws-backup-system
        resource: buttons
        verb: GET
