# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-backup-backupvault
  namespace: aws-backup-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Storage
spec:
  apiRef:
    name: aws-backup-backupvault-card-data
    namespace: aws-backup-system
  widgetData:
    title: Aws Backup Backupvault
    headerLeft: Provision an AWS Backup BackupVault via the ACK backup controller.
    tooltip: Aws Backup Backupvault blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Storage
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-backup-backupvault-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Storage", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-backup-backupvault-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-backup-backupvault-install
        namespace: aws-backup-system
        resource: buttons
        verb: GET
