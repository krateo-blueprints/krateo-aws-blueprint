# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-bedrock-inferenceprofile
  namespace: aws-bedrock-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: MachineLearning
spec:
  apiRef:
    name: aws-bedrock-inferenceprofile-card-data
    namespace: aws-bedrock-system
  widgetData:
    title: Aws Bedrock Inferenceprofile
    headerLeft: Provision an AWS Bedrock InferenceProfile via the ACK bedrock controller.
    tooltip: Aws Bedrock Inferenceprofile blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - MachineLearning
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-bedrock-inferenceprofile-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["MachineLearning", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-bedrock-inferenceprofile-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-bedrock-inferenceprofile-install
        namespace: aws-bedrock-system
        resource: buttons
        verb: GET
