# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-bedrockagent-agent
  namespace: aws-bedrockagent-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-bedrockagent-agent-card-data
    namespace: aws-bedrockagent-system
  widgetData:
    title: Aws Bedrockagent Agent
    headerLeft: Provision an AWS Bedrockagent Agent via the ACK bedrockagent controller.
    tooltip: Aws Bedrockagent Agent blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-bedrockagent-agent-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-bedrockagent-agent-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-bedrockagent-agent-install
        namespace: aws-bedrockagent-system
        resource: buttons
        verb: GET
