# Krateo Blueprint — AWS Bedrockagentcorecontrol AgentRuntime

> Generated by `tools/ackgen` from the ACK `AgentRuntime` CRD (`bedrockagentcorecontrol.services.k8s.aws`).

Provisions an AWS Bedrockagentcorecontrol **AgentRuntime** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
bedrockagentcorecontrol controller. The chart renders a native `bedrockagentcorecontrol.services.k8s.aws/v1alpha1` `AgentRuntime`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsBedrockagentcorecontrolAgentruntime` Composition you create
becomes an AWS AgentRuntime.

## Prerequisites

- **ACK bedrockagentcorecontrol controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-bedrockagentcorecontrol-controller \
    oci://public.ecr.aws/aws-controllers-k8s/bedrockagentcorecontrol-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK AgentRuntime spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-bedrockagentcorecontrol-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsBedrockagentcorecontrolAgentruntime type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsBedrockagentcorecontrolAgentruntime` Composition type (`composition.krateo.io/v0-1-1`,
plural `awsbedrockagentcorecontrolagentruntimes`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-bedrockagentcorecontrol-agentruntime`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsBedrockagentcorecontrolAgentruntime
metadata:
  name: my-agentruntime
  namespace: aws-bedrockagentcorecontrol-system
spec:
  region: eu-west-1
  # ... ACK AgentRuntime spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsbedrockagentcorecontrolagentruntimes.composition.krateo.io -n aws-bedrockagentcorecontrol-system
kubectl get agentruntimes.bedrockagentcorecontrol.services.k8s.aws -A
```
