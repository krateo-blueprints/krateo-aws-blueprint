{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Provision an AWS Bedrockagentcorecontrol AgentRuntimeEndpoint via the ACK bedrockagentcorecontrol controller. Fields mirror the bedrockagentcorecontrol.services.k8s.aws/v1alpha1 AgentRuntimeEndpoint spec.",
  "properties": {
    "agentRuntimeID": {
      "description": "The unique identifier of the AgentCore Runtime to create an endpoint for.\n\nRegex Pattern: `^[a-zA-Z][a-zA-Z0-9_]{0,99}-[a-zA-Z0-9]{10}$`",
      "type": "string"
    },
    "agentRuntimeVersion": {
      "description": "The version of the AgentCore Runtime to use for the endpoint.\n\nRegex Pattern: `^([1-9][0-9]{0,4})$`",
      "type": "string"
    },
    "description": {
      "description": "The description of the AgentCore Runtime endpoint.",
      "type": "string"
    },
    "name": {
      "description": "The name of the AgentCore Runtime endpoint.\n\nRegex Pattern: `^[a-zA-Z][a-zA-Z0-9_]{0,47}$`",
      "type": "string"
    },
    "region": {
      "default": "",
      "description": "AWS region for this resource (e.g. eu-west-1). Rendered as the services.k8s.aws/region annotation. Empty inherits the ACK controller's default region.",
      "title": "AWS region",
      "type": "string"
    },
    "tags": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "A map of tag keys and values to assign to the agent runtime endpoint. Tags\nenable you to categorize your resources in different ways, for example, by\npurpose, owner, or environment.",
      "type": "object"
    }
  },
  "required": [
    "agentRuntimeID"
  ],
  "title": "AWS Bedrockagentcorecontrol AgentRuntimeEndpoint",
  "type": "object"
}
