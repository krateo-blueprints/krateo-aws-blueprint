apiVersion: v2
# Generated by tools/ackgen from the ACK GatewayTarget CRD (bedrockagentcorecontrol.services.k8s.aws).
#
# Renders a native bedrockagentcorecontrol.services.k8s.aws/v1alpha1 GatewayTarget resource; the ACK bedrockagentcorecontrol
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsBedrockagentcorecontrolGatewaytarget (plural awsbedrockagentcorecontrolgatewaytargets) in the composition.krateo.io group.
# The Composition spec mirrors the ACK GatewayTarget spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-bedrockagentcorecontrol-gatewaytarget
description: A Krateo blueprint that provisions an AWS Bedrockagentcorecontrol GatewayTarget via the AWS Controllers for Kubernetes (ACK) bedrockagentcorecontrol controller. Requires the ACK bedrockagentcorecontrol controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - bedrockagentcorecontrol
  - gatewaytarget
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
