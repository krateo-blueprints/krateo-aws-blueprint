# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudfront-cachepolicy
  namespace: aws-cloudfront-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-cloudfront-cachepolicy-card-data
    namespace: aws-cloudfront-system
  widgetData:
    title: Aws Cloudfront Cachepolicy
    headerLeft: Provision an AWS Cloudfront CachePolicy via the ACK cloudfront controller.
    tooltip: Aws Cloudfront Cachepolicy blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudfront-cachepolicy-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudfront-cachepolicy-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudfront-cachepolicy-install
        namespace: aws-cloudfront-system
        resource: buttons
        verb: GET
