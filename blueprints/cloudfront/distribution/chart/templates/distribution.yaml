{{- /*
Renders a native ACK Distribution custom resource from the Composition spec.

The Composition spec (.Values) mirrors the ACK Distribution spec, so it passes through verbatim.
`region` is Krateo-wiring (not part of the ACK spec) and becomes the standard ACK region
annotation. Generated by tools/ackgen.
*/ -}}
apiVersion: cloudfront.services.k8s.aws/v1alpha1
kind: Distribution
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aws-cloudfront-distribution.labels" . | nindent 4 }}
  {{- with .Values.region }}
  annotations:
    services.k8s.aws/region: {{ . | quote }}
  {{- end }}
{{- $spec := omit .Values "region" }}
{{- if $spec }}
spec:
{{ toYaml $spec | indent 2 }}
{{- else }}
spec: {}
{{- end }}
