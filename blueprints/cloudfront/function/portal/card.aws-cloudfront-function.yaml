# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudfront-function
  namespace: aws-cloudfront-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-cloudfront-function-card-data
    namespace: aws-cloudfront-system
  widgetData:
    title: Aws Cloudfront Function
    headerLeft: Provision an AWS Cloudfront Function via the ACK cloudfront controller.
    tooltip: Aws Cloudfront Function blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudfront-function-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudfront-function-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudfront-function-install
        namespace: aws-cloudfront-system
        resource: buttons
        verb: GET
