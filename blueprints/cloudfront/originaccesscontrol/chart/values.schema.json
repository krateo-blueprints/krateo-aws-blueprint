{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Provision an AWS Cloudfront OriginAccessControl via the ACK cloudfront controller. Fields mirror the cloudfront.services.k8s.aws/v1alpha1 OriginAccessControl spec.",
  "properties": {
    "originAccessControlConfig": {
      "description": "Contains the origin access control.",
      "properties": {
        "description": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "originAccessControlOriginType": {
          "type": "string"
        },
        "signingBehavior": {
          "type": "string"
        },
        "signingProtocol": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "region": {
      "default": "",
      "description": "AWS region for this resource (e.g. eu-west-1). Rendered as the services.k8s.aws/region annotation. Empty inherits the ACK controller's default region.",
      "title": "AWS region",
      "type": "string"
    }
  },
  "required": [
    "originAccessControlConfig"
  ],
  "title": "AWS Cloudfront OriginAccessControl",
  "type": "object"
}
