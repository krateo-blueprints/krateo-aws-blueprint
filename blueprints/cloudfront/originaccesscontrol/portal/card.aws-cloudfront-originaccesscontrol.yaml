# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudfront-originaccesscontrol
  namespace: aws-cloudfront-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-cloudfront-originaccesscontrol-card-data
    namespace: aws-cloudfront-system
  widgetData:
    title: Aws Cloudfront Originaccesscontrol
    headerLeft: Provision an AWS Cloudfront OriginAccessControl via the ACK cloudfront controller.
    tooltip: Aws Cloudfront Originaccesscontrol blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudfront-originaccesscontrol-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudfront-originaccesscontrol-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudfront-originaccesscontrol-install
        namespace: aws-cloudfront-system
        resource: buttons
        verb: GET
