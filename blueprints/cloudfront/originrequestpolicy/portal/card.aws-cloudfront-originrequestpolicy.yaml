# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudfront-originrequestpolicy
  namespace: aws-cloudfront-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-cloudfront-originrequestpolicy-card-data
    namespace: aws-cloudfront-system
  widgetData:
    title: Aws Cloudfront Originrequestpolicy
    headerLeft: Provision an AWS Cloudfront OriginRequestPolicy via the ACK cloudfront controller.
    tooltip: Aws Cloudfront Originrequestpolicy blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudfront-originrequestpolicy-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudfront-originrequestpolicy-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudfront-originrequestpolicy-install
        namespace: aws-cloudfront-system
        resource: buttons
        verb: GET
