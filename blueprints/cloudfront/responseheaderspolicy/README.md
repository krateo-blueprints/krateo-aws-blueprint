# Krateo Blueprint — AWS Cloudfront ResponseHeadersPolicy

> Generated by `tools/ackgen` from the ACK `ResponseHeadersPolicy` CRD (`cloudfront.services.k8s.aws`).

Provisions an AWS Cloudfront **ResponseHeadersPolicy** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
cloudfront controller. The chart renders a native `cloudfront.services.k8s.aws/v1alpha1` `ResponseHeadersPolicy`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsCloudfrontResponseheaderspolicy` Composition you create
becomes an AWS ResponseHeadersPolicy.

## Prerequisites

- **ACK cloudfront controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-cloudfront-controller \
    oci://public.ecr.aws/aws-controllers-k8s/cloudfront-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK ResponseHeadersPolicy spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-cloudfront-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsCloudfrontResponseheaderspolicy type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsCloudfrontResponseheaderspolicy` Composition type (`composition.krateo.io/v0-1-0`,
plural `awscloudfrontresponseheaderspolicies`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-cloudfront-responseheaderspolicy`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsCloudfrontResponseheaderspolicy
metadata:
  name: my-responseheaderspolicy
  namespace: aws-cloudfront-system
spec:
  region: eu-west-1
  # ... ACK ResponseHeadersPolicy spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awscloudfrontresponseheaderspolicies.composition.krateo.io -n aws-cloudfront-system
kubectl get responseheaderspolicys.cloudfront.services.k8s.aws -A
```
