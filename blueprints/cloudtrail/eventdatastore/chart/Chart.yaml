apiVersion: v2
# Generated by tools/ackgen from the ACK EventDataStore CRD (cloudtrail.services.k8s.aws).
#
# Renders a native cloudtrail.services.k8s.aws/v1alpha1 EventDataStore resource; the ACK cloudtrail
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsCloudtrailEventdatastore (plural awscloudtraileventdatastores) in the composition.krateo.io group.
# The Composition spec mirrors the ACK EventDataStore spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-cloudtrail-eventdatastore
description: A Krateo blueprint that provisions an AWS Cloudtrail EventDataStore via the AWS Controllers for Kubernetes (ACK) cloudtrail controller. Requires the ACK cloudtrail controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - cloudtrail
  - eventdatastore
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
