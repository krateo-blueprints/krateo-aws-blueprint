apiVersion: v2
# Generated by tools/ackgen from the ACK Trail CRD (cloudtrail.services.k8s.aws).
#
# Renders a native cloudtrail.services.k8s.aws/v1alpha1 Trail resource; the ACK cloudtrail
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsCloudtrailTrail (plural awscloudtrailtrails) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Trail spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-cloudtrail-trail
description: A Krateo blueprint that provisions an AWS Cloudtrail Trail via the AWS Controllers for Kubernetes (ACK) cloudtrail controller. Requires the ACK cloudtrail controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - cloudtrail
  - trail
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "observability"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
