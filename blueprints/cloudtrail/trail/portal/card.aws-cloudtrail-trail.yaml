# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudtrail-trail
  namespace: aws-cloudtrail-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-cloudtrail-trail-card-data
    namespace: aws-cloudtrail-system
  widgetData:
    title: Aws Cloudtrail Trail
    headerLeft: Provision an AWS Cloudtrail Trail via the ACK cloudtrail controller.
    tooltip: Aws Cloudtrail Trail blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudtrail-trail-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudtrail-trail-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudtrail-trail-install
        namespace: aws-cloudtrail-system
        resource: buttons
        verb: GET
