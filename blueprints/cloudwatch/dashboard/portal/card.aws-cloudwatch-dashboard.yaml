# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudwatch-dashboard
  namespace: aws-cloudwatch-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-cloudwatch-dashboard-card-data
    namespace: aws-cloudwatch-system
  widgetData:
    title: Aws Cloudwatch Dashboard
    headerLeft: Provision an AWS Cloudwatch Dashboard via the ACK cloudwatch controller.
    tooltip: Aws Cloudwatch Dashboard blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudwatch-dashboard-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudwatch-dashboard-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudwatch-dashboard-install
        namespace: aws-cloudwatch-system
        resource: buttons
        verb: GET
