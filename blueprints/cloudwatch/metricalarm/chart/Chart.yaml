apiVersion: v2
# Generated by tools/ackgen from the ACK MetricAlarm CRD (cloudwatch.services.k8s.aws).
#
# Renders a native cloudwatch.services.k8s.aws/v1alpha1 MetricAlarm resource; the ACK cloudwatch
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsCloudwatchMetricalarm (plural awscloudwatchmetricalarms) in the composition.krateo.io group.
# The Composition spec mirrors the ACK MetricAlarm spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-cloudwatch-metricalarm
description: A Krateo blueprint that provisions an AWS Cloudwatch MetricAlarm via the AWS Controllers for Kubernetes (ACK) cloudwatch controller. Requires the ACK cloudwatch controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - cloudwatch
  - metricalarm
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "observability"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
