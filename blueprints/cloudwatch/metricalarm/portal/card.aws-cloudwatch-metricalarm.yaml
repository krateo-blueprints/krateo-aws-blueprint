# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudwatch-metricalarm
  namespace: aws-cloudwatch-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-cloudwatch-metricalarm-card-data
    namespace: aws-cloudwatch-system
  widgetData:
    title: Aws Cloudwatch Metricalarm
    headerLeft: Provision an AWS Cloudwatch MetricAlarm via the ACK cloudwatch controller.
    tooltip: Aws Cloudwatch Metricalarm blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudwatch-metricalarm-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudwatch-metricalarm-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudwatch-metricalarm-install
        namespace: aws-cloudwatch-system
        resource: buttons
        verb: GET
