# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudwatch-metricstream
  namespace: aws-cloudwatch-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-cloudwatch-metricstream-card-data
    namespace: aws-cloudwatch-system
  widgetData:
    title: Aws Cloudwatch Metricstream
    headerLeft: Provision an AWS Cloudwatch MetricStream via the ACK cloudwatch controller.
    tooltip: Aws Cloudwatch Metricstream blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudwatch-metricstream-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudwatch-metricstream-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudwatch-metricstream-install
        namespace: aws-cloudwatch-system
        resource: buttons
        verb: GET
