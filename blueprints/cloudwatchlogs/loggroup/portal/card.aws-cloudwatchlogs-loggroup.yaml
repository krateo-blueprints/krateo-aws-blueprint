# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-cloudwatchlogs-loggroup
  namespace: aws-cloudwatchlogs-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-cloudwatchlogs-loggroup-card-data
    namespace: aws-cloudwatchlogs-system
  widgetData:
    title: Aws Cloudwatchlogs Loggroup
    headerLeft: Provision an AWS Cloudwatchlogs LogGroup via the ACK cloudwatchlogs controller.
    tooltip: Aws Cloudwatchlogs Loggroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-cloudwatchlogs-loggroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-cloudwatchlogs-loggroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-cloudwatchlogs-loggroup-install
        namespace: aws-cloudwatchlogs-system
        resource: buttons
        verb: GET
