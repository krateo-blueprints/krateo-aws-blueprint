# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-codeartifact-domain
  namespace: aws-codeartifact-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-codeartifact-domain-card-data
    namespace: aws-codeartifact-system
  widgetData:
    title: Aws Codeartifact Domain
    headerLeft: Provision an AWS Codeartifact Domain via the ACK codeartifact controller.
    tooltip: Aws Codeartifact Domain blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-codeartifact-domain-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-codeartifact-domain-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-codeartifact-domain-install
        namespace: aws-codeartifact-system
        resource: buttons
        verb: GET
