apiVersion: v2
# Generated by tools/ackgen from the ACK PackageGroup CRD (codeartifact.services.k8s.aws).
#
# Renders a native codeartifact.services.k8s.aws/v1alpha1 PackageGroup resource; the ACK codeartifact
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsCodeartifactPackagegroup (plural awscodeartifactpackagegroups) in the composition.krateo.io group.
# The Composition spec mirrors the ACK PackageGroup spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-codeartifact-packagegroup
description: A Krateo blueprint that provisions an AWS Codeartifact PackageGroup via the AWS Controllers for Kubernetes (ACK) codeartifact controller. Requires the ACK codeartifact controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - codeartifact
  - packagegroup
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
