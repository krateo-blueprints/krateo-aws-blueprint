# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-dms-certificate
  namespace: aws-dms-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-dms-certificate-card-data
    namespace: aws-dms-system
  widgetData:
    title: Aws Dms Certificate
    headerLeft: Provision an AWS Dms Certificate via the ACK dms controller.
    tooltip: Aws Dms Certificate blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-dms-certificate-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-dms-certificate-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-dms-certificate-install
        namespace: aws-dms-system
        resource: buttons
        verb: GET
