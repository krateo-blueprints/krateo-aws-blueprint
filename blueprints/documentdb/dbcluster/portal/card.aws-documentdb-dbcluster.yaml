# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-documentdb-dbcluster
  namespace: aws-documentdb-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-documentdb-dbcluster-card-data
    namespace: aws-documentdb-system
  widgetData:
    title: Aws Documentdb Dbcluster
    headerLeft: Provision an AWS Documentdb DBCluster via the ACK documentdb controller.
    tooltip: Aws Documentdb Dbcluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-documentdb-dbcluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-documentdb-dbcluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-documentdb-dbcluster-install
        namespace: aws-documentdb-system
        resource: buttons
        verb: GET
