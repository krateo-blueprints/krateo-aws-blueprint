# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-documentdb-dbsubnetgroup
  namespace: aws-documentdb-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-documentdb-dbsubnetgroup-card-data
    namespace: aws-documentdb-system
  widgetData:
    title: Aws Documentdb Dbsubnetgroup
    headerLeft: Provision an AWS Documentdb DBSubnetGroup via the ACK documentdb controller.
    tooltip: Aws Documentdb Dbsubnetgroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-documentdb-dbsubnetgroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-documentdb-dbsubnetgroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-documentdb-dbsubnetgroup-install
        namespace: aws-documentdb-system
        resource: buttons
        verb: GET
