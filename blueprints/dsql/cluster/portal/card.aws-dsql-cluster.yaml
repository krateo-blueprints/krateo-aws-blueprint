# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-dsql-cluster
  namespace: aws-dsql-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-dsql-cluster-card-data
    namespace: aws-dsql-system
  widgetData:
    title: Aws Dsql Cluster
    headerLeft: Provision an AWS Dsql Cluster via the ACK dsql controller.
    tooltip: Aws Dsql Cluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-dsql-cluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-dsql-cluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-dsql-cluster-install
        namespace: aws-dsql-system
        resource: buttons
        verb: GET
