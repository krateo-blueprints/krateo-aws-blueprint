# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-dynamodb-backup
  namespace: aws-dynamodb-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-dynamodb-backup-card-data
    namespace: aws-dynamodb-system
  widgetData:
    title: Aws Dynamodb Backup
    headerLeft: Provision an AWS Dynamodb Backup via the ACK dynamodb controller.
    tooltip: Aws Dynamodb Backup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-dynamodb-backup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-dynamodb-backup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-dynamodb-backup-install
        namespace: aws-dynamodb-system
        resource: buttons
        verb: GET
