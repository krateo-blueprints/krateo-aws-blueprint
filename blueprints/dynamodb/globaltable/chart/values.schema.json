{
  "$schema": "http://json-schema.org/draft-07/schema",
  "additionalProperties": false,
  "description": "Provision an AWS Dynamodb GlobalTable via the ACK dynamodb controller. Fields mirror the dynamodb.services.k8s.aws/v1alpha1 GlobalTable spec.",
  "properties": {
    "globalTableName": {
      "description": "The global table name.\n\nRegex Pattern: `^[a-zA-Z0-9_.-]+$`",
      "type": "string"
    },
    "region": {
      "default": "",
      "description": "AWS region for this resource (e.g. eu-west-1). Rendered as the services.k8s.aws/region annotation. Empty inherits the ACK controller's default region.",
      "title": "AWS region",
      "type": "string"
    },
    "replicationGroup": {
      "description": "The Regions where the global table needs to be created.",
      "items": {
        "description": "Represents the properties of a replica.",
        "properties": {
          "regionName": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "type": "array"
    }
  },
  "required": [
    "globalTableName",
    "replicationGroup"
  ],
  "title": "AWS Dynamodb GlobalTable",
  "type": "object"
}
