# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-dynamodb-globaltable
  namespace: aws-dynamodb-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-dynamodb-globaltable-card-data
    namespace: aws-dynamodb-system
  widgetData:
    title: Aws Dynamodb Globaltable
    headerLeft: Provision an AWS Dynamodb GlobalTable via the ACK dynamodb controller.
    tooltip: Aws Dynamodb Globaltable blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-dynamodb-globaltable-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-dynamodb-globaltable-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-dynamodb-globaltable-install
        namespace: aws-dynamodb-system
        resource: buttons
        verb: GET
