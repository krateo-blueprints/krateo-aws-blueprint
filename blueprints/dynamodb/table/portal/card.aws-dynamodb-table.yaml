# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-dynamodb-table
  namespace: aws-dynamodb-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-dynamodb-table-card-data
    namespace: aws-dynamodb-system
  widgetData:
    title: Aws Dynamodb Table
    headerLeft: Provision an AWS Dynamodb Table via the ACK dynamodb controller.
    tooltip: Aws Dynamodb Table blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-dynamodb-table-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-dynamodb-table-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-dynamodb-table-install
        namespace: aws-dynamodb-system
        resource: buttons
        verb: GET
