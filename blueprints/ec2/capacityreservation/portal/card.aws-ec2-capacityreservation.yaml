# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ec2-capacityreservation
  namespace: aws-ec2-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-ec2-capacityreservation-card-data
    namespace: aws-ec2-system
  widgetData:
    title: Aws Ec2 Capacityreservation
    headerLeft: Provision an AWS Ec2 CapacityReservation via the ACK ec2 controller.
    tooltip: Aws Ec2 Capacityreservation blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ec2-capacityreservation-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ec2-capacityreservation-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ec2-capacityreservation-install
        namespace: aws-ec2-system
        resource: buttons
        verb: GET
