# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ec2-managedprefixlist
  namespace: aws-ec2-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Compute
spec:
  apiRef:
    name: aws-ec2-managedprefixlist-card-data
    namespace: aws-ec2-system
  widgetData:
    title: Aws Ec2 Managedprefixlist
    headerLeft: Provision an AWS Ec2 ManagedPrefixList via the ACK ec2 controller.
    tooltip: Aws Ec2 Managedprefixlist blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Compute
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ec2-managedprefixlist-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Compute", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ec2-managedprefixlist-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ec2-managedprefixlist-install
        namespace: aws-ec2-system
        resource: buttons
        verb: GET
