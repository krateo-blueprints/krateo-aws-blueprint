# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ec2-networkacl
  namespace: aws-ec2-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Compute
spec:
  apiRef:
    name: aws-ec2-networkacl-card-data
    namespace: aws-ec2-system
  widgetData:
    title: Aws Ec2 Networkacl
    headerLeft: Provision an AWS Ec2 NetworkACL via the ACK ec2 controller.
    tooltip: Aws Ec2 Networkacl blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Compute
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ec2-networkacl-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Compute", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ec2-networkacl-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ec2-networkacl-install
        namespace: aws-ec2-system
        resource: buttons
        verb: GET
