# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ec2-vpcpeeringconnection
  namespace: aws-ec2-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Compute
spec:
  apiRef:
    name: aws-ec2-vpcpeeringconnection-card-data
    namespace: aws-ec2-system
  widgetData:
    title: Aws Ec2 Vpcpeeringconnection
    headerLeft: Provision an AWS Ec2 VPCPeeringConnection via the ACK ec2 controller.
    tooltip: Aws Ec2 Vpcpeeringconnection blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Compute
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ec2-vpcpeeringconnection-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Compute", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ec2-vpcpeeringconnection-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ec2-vpcpeeringconnection-install
        namespace: aws-ec2-system
        resource: buttons
        verb: GET
