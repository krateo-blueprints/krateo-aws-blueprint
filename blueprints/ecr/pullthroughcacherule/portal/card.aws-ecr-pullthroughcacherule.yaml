# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ecr-pullthroughcacherule
  namespace: aws-ecr-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-ecr-pullthroughcacherule-card-data
    namespace: aws-ecr-system
  widgetData:
    title: Aws Ecr Pullthroughcacherule
    headerLeft: Provision an AWS Ecr PullThroughCacheRule via the ACK ecr controller.
    tooltip: Aws Ecr Pullthroughcacherule blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ecr-pullthroughcacherule-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ecr-pullthroughcacherule-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ecr-pullthroughcacherule-install
        namespace: aws-ecr-system
        resource: buttons
        verb: GET
