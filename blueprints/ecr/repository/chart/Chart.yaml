apiVersion: v2
# Generated by tools/ackgen from the ACK Repository CRD (ecr.services.k8s.aws).
#
# Renders a native ecr.services.k8s.aws/v1alpha1 Repository resource; the ACK ecr
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsEcrRepository (plural awsecrrepositories) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Repository spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-ecr-repository
description: A Krateo blueprint that provisions an AWS Ecr Repository via the AWS Controllers for Kubernetes (ACK) ecr controller. Requires the ACK ecr controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - ecr
  - repository
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
