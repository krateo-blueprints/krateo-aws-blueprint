# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ecr-repositorycreationtemplate
  namespace: aws-ecr-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Containers
spec:
  apiRef:
    name: aws-ecr-repositorycreationtemplate-card-data
    namespace: aws-ecr-system
  widgetData:
    title: Aws Ecr Repositorycreationtemplate
    headerLeft: Provision an AWS Ecr RepositoryCreationTemplate via the ACK ecr controller.
    tooltip: Aws Ecr Repositorycreationtemplate blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Containers
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ecr-repositorycreationtemplate-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Containers", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ecr-repositorycreationtemplate-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ecr-repositorycreationtemplate-install
        namespace: aws-ecr-system
        resource: buttons
        verb: GET
