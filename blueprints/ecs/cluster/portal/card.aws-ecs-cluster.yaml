# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ecs-cluster
  namespace: aws-ecs-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-ecs-cluster-card-data
    namespace: aws-ecs-system
  widgetData:
    title: Aws Ecs Cluster
    headerLeft: Provision an AWS Ecs Cluster via the ACK ecs controller.
    tooltip: Aws Ecs Cluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ecs-cluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ecs-cluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ecs-cluster-install
        namespace: aws-ecs-system
        resource: buttons
        verb: GET
