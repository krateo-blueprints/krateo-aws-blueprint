# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ecs-service
  namespace: aws-ecs-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Containers
spec:
  apiRef:
    name: aws-ecs-service-card-data
    namespace: aws-ecs-system
  widgetData:
    title: Aws Ecs Service
    headerLeft: Provision an AWS Ecs Service via the ACK ecs controller.
    tooltip: Aws Ecs Service blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Containers
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ecs-service-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Containers", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ecs-service-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ecs-service-install
        namespace: aws-ecs-system
        resource: buttons
        verb: GET
