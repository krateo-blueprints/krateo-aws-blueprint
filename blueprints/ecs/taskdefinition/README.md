# Krateo Blueprint — AWS Ecs TaskDefinition

> Generated by `tools/ackgen` from the ACK `TaskDefinition` CRD (`ecs.services.k8s.aws`).

Provisions an AWS Ecs **TaskDefinition** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
ecs controller. The chart renders a native `ecs.services.k8s.aws/v1alpha1` `TaskDefinition`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsEcsTaskdefinition` Composition you create
becomes an AWS TaskDefinition.

## Prerequisites

- **ACK ecs controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-ecs-controller \
    oci://public.ecr.aws/aws-controllers-k8s/ecs-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK TaskDefinition spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-ecs-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsEcsTaskdefinition type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsEcsTaskdefinition` Composition type (`composition.krateo.io/v0-1-1`,
plural `awsecstaskdefinitions`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-ecs-taskdefinition`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsEcsTaskdefinition
metadata:
  name: my-taskdefinition
  namespace: aws-ecs-system
spec:
  region: eu-west-1
  # ... ACK TaskDefinition spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsecstaskdefinitions.composition.krateo.io -n aws-ecs-system
kubectl get taskdefinitions.ecs.services.k8s.aws -A
```
