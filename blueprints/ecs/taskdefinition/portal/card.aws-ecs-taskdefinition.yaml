# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ecs-taskdefinition
  namespace: aws-ecs-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Containers
spec:
  apiRef:
    name: aws-ecs-taskdefinition-card-data
    namespace: aws-ecs-system
  widgetData:
    title: Aws Ecs Taskdefinition
    headerLeft: Provision an AWS Ecs TaskDefinition via the ACK ecs controller.
    tooltip: Aws Ecs Taskdefinition blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Containers
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ecs-taskdefinition-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Containers", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ecs-taskdefinition-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ecs-taskdefinition-install
        namespace: aws-ecs-system
        resource: buttons
        verb: GET
