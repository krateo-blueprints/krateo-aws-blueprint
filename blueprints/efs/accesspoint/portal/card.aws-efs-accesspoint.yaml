# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-efs-accesspoint
  namespace: aws-efs-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Storage
spec:
  apiRef:
    name: aws-efs-accesspoint-card-data
    namespace: aws-efs-system
  widgetData:
    title: Aws Efs Accesspoint
    headerLeft: Provision an AWS Efs AccessPoint via the ACK efs controller.
    tooltip: Aws Efs Accesspoint blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Storage
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-efs-accesspoint-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Storage", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-efs-accesspoint-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-efs-accesspoint-install
        namespace: aws-efs-system
        resource: buttons
        verb: GET
