# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Form
metadata:
  name: aws-efs-accesspoint-create
  namespace: aws-efs-system
spec:
  apiRef:
    name: aws-efs-accesspoint-formdef
    namespace: aws-efs-system
  widgetData:
    layout: vertical
    submitActionId: submit
    propertiesToHide: []
    schema: {}
    buttonConfig:
      primary:
        label: Review & create
      secondary:
        label: Cancel
    actions:
      rest:
        - id: submit
          type: rest
          resourceRefId: create-composition
          headers:
            - 'Content-Type: application/json'
          onSuccessNavigateTo: /compositions
          successMessage: Aws Efs Accesspoint composition created
          payloadToOverride:
            - name: spec
              value: ${ .json }
  widgetDataTemplate:
    - forPath: schema
      expression: ${ .schemaSpec }
  resourcesRefs:
    items:
      - id: create-composition
        apiVersion: composition.krateo.io/v0-1-1
        resource: awsefsaccesspoints
        namespace: aws-efs-system
        verb: POST
        payload:
          apiVersion: composition.krateo.io/v0-1-1
          kind: Awsefsaccesspoint
          metadata:
            namespace: aws-efs-system
