# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-efs-filesystem
  namespace: aws-efs-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-efs-filesystem-card-data
    namespace: aws-efs-system
  widgetData:
    title: Aws Efs Filesystem
    headerLeft: Provision an AWS Efs FileSystem via the ACK efs controller.
    tooltip: Aws Efs Filesystem blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-efs-filesystem-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-efs-filesystem-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-efs-filesystem-install
        namespace: aws-efs-system
        resource: buttons
        verb: GET
