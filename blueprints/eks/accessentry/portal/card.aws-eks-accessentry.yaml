# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eks-accessentry
  namespace: aws-eks-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-eks-accessentry-card-data
    namespace: aws-eks-system
  widgetData:
    title: Aws Eks Accessentry
    headerLeft: Provision an AWS Eks AccessEntry via the ACK eks controller.
    tooltip: Aws Eks Accessentry blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eks-accessentry-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eks-accessentry-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eks-accessentry-install
        namespace: aws-eks-system
        resource: buttons
        verb: GET
