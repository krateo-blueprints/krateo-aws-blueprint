# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eks-cluster
  namespace: aws-eks-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Containers
spec:
  apiRef:
    name: aws-eks-cluster-card-data
    namespace: aws-eks-system
  widgetData:
    title: Aws Eks Cluster
    headerLeft: Provision an AWS Eks Cluster via the ACK eks controller.
    tooltip: Aws Eks Cluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Containers
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eks-cluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Containers", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eks-cluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eks-cluster-install
        namespace: aws-eks-system
        resource: buttons
        verb: GET
