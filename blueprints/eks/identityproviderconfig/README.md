# Krateo Blueprint — AWS Eks IdentityProviderConfig

> Generated by `tools/ackgen` from the ACK `IdentityProviderConfig` CRD (`eks.services.k8s.aws`).

Provisions an AWS Eks **IdentityProviderConfig** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
eks controller. The chart renders a native `eks.services.k8s.aws/v1alpha1` `IdentityProviderConfig`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsEksIdentityproviderconfig` Composition you create
becomes an AWS IdentityProviderConfig.

## Prerequisites

- **ACK eks controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-eks-controller \
    oci://public.ecr.aws/aws-controllers-k8s/eks-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK IdentityProviderConfig spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-eks-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsEksIdentityproviderconfig type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsEksIdentityproviderconfig` Composition type (`composition.krateo.io/v0-1-1`,
plural `awseksidentityproviderconfigs`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-eks-identityproviderconfig`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsEksIdentityproviderconfig
metadata:
  name: my-identityproviderconfig
  namespace: aws-eks-system
spec:
  region: eu-west-1
  # ... ACK IdentityProviderConfig spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awseksidentityproviderconfigs.composition.krateo.io -n aws-eks-system
kubectl get identityproviderconfigs.eks.services.k8s.aws -A
```
