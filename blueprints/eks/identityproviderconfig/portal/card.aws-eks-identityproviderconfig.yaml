# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eks-identityproviderconfig
  namespace: aws-eks-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-eks-identityproviderconfig-card-data
    namespace: aws-eks-system
  widgetData:
    title: Aws Eks Identityproviderconfig
    headerLeft: Provision an AWS Eks IdentityProviderConfig via the ACK eks controller.
    tooltip: Aws Eks Identityproviderconfig blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eks-identityproviderconfig-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eks-identityproviderconfig-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eks-identityproviderconfig-install
        namespace: aws-eks-system
        resource: buttons
        verb: GET
