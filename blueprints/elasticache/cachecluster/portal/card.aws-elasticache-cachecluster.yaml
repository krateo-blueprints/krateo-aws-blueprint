# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-elasticache-cachecluster
  namespace: aws-elasticache-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-elasticache-cachecluster-card-data
    namespace: aws-elasticache-system
  widgetData:
    title: Aws Elasticache Cachecluster
    headerLeft: Provision an AWS Elasticache CacheCluster via the ACK elasticache controller.
    tooltip: Aws Elasticache Cachecluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-elasticache-cachecluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-elasticache-cachecluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-elasticache-cachecluster-install
        namespace: aws-elasticache-system
        resource: buttons
        verb: GET
