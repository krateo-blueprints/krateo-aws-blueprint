# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-elasticache-cacheparametergroup
  namespace: aws-elasticache-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-elasticache-cacheparametergroup-card-data
    namespace: aws-elasticache-system
  widgetData:
    title: Aws Elasticache Cacheparametergroup
    headerLeft: Provision an AWS Elasticache CacheParameterGroup via the ACK elasticache controller.
    tooltip: Aws Elasticache Cacheparametergroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-elasticache-cacheparametergroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-elasticache-cacheparametergroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-elasticache-cacheparametergroup-install
        namespace: aws-elasticache-system
        resource: buttons
        verb: GET
