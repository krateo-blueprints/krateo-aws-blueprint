apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-elasticache-cachesubnetgroup-card
  namespace: aws-elasticache-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-elasticache-cachesubnetgroup-card
  namespace: aws-elasticache-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-elasticache-cachesubnetgroup-customform
      namespace: aws-elasticache-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-elasticache-cachesubnetgroup
      namespace: aws-elasticache-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Elasticache CacheSubnetGroup via the ACK elasticache controller.
      icon: fa-brands fa-aws
      tags: aws-elasticache-system,0.1.1
      title: aws-elasticache-cachesubnetgroup
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-elasticache-cachesubnetgroup-card
    namespace: aws-elasticache-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-elasticache-system/compositiondefinitions/aws-elasticache-cachesubnetgroup"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-elasticache-cachesubnetgroup-customform
  namespace: aws-elasticache-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-elasticache-cachesubnetgroup-customform
  namespace: aws-elasticache-system
spec:
  type: customform
  propsRef:
    name: template-aws-elasticache-cachesubnetgroup-customform
    namespace: aws-elasticache-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-1
        verb: POST
        resource: awselasticachecachesubnetgroups
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-elasticache-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-1") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awselasticachecachesubnetgroups.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
