# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-elasticache-serverlesscache
  namespace: aws-elasticache-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-elasticache-serverlesscache-card-data
    namespace: aws-elasticache-system
  widgetData:
    title: Aws Elasticache Serverlesscache
    headerLeft: Provision an AWS Elasticache ServerlessCache via the ACK elasticache controller.
    tooltip: Aws Elasticache Serverlesscache blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-elasticache-serverlesscache-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-elasticache-serverlesscache-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-elasticache-serverlesscache-install
        namespace: aws-elasticache-system
        resource: buttons
        verb: GET
