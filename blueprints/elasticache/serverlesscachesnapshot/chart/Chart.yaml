apiVersion: v2
# Generated by tools/ackgen from the ACK ServerlessCacheSnapshot CRD (elasticache.services.k8s.aws).
#
# Renders a native elasticache.services.k8s.aws/v1alpha1 ServerlessCacheSnapshot resource; the ACK elasticache
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsElasticacheServerlesscachesnapshot (plural awselasticacheserverlesscachesnapshots) in the composition.krateo.io group.
# The Composition spec mirrors the ACK ServerlessCacheSnapshot spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-elasticache-serverlesscachesnapshot
description: A Krateo blueprint that provisions an AWS Elasticache ServerlessCacheSnapshot via the AWS Controllers for Kubernetes (ACK) elasticache controller. Requires the ACK elasticache controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - elasticache
  - serverlesscachesnapshot
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "database"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
