# Krateo Blueprint — AWS Elasticache User

> Generated by `tools/ackgen` from the ACK `User` CRD (`elasticache.services.k8s.aws`).

Provisions an AWS Elasticache **User** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
elasticache controller. The chart renders a native `elasticache.services.k8s.aws/v1alpha1` `User`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsElasticacheUser` Composition you create
becomes an AWS User.

## Prerequisites

- **ACK elasticache controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-elasticache-controller \
    oci://public.ecr.aws/aws-controllers-k8s/elasticache-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK User spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-elasticache-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsElasticacheUser type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsElasticacheUser` Composition type (`composition.krateo.io/v0-1-1`,
plural `awselasticacheusers`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-elasticache-user`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsElasticacheUser
metadata:
  name: my-user
  namespace: aws-elasticache-system
spec:
  region: eu-west-1
  # ... ACK User spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awselasticacheusers.composition.krateo.io -n aws-elasticache-system
kubectl get users.elasticache.services.k8s.aws -A
```
