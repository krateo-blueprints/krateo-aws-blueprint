# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-elbv2-rule
  namespace: aws-elbv2-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-elbv2-rule-card-data
    namespace: aws-elbv2-system
  widgetData:
    title: Aws Elbv2 Rule
    headerLeft: Provision an AWS Elbv2 Rule via the ACK elbv2 controller.
    tooltip: Aws Elbv2 Rule blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-elbv2-rule-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-elbv2-rule-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-elbv2-rule-install
        namespace: aws-elbv2-system
        resource: buttons
        verb: GET
