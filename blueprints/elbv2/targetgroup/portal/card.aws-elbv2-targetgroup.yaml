# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-elbv2-targetgroup
  namespace: aws-elbv2-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-elbv2-targetgroup-card-data
    namespace: aws-elbv2-system
  widgetData:
    title: Aws Elbv2 Targetgroup
    headerLeft: Provision an AWS Elbv2 TargetGroup via the ACK elbv2 controller.
    tooltip: Aws Elbv2 Targetgroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-elbv2-targetgroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-elbv2-targetgroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-elbv2-targetgroup-install
        namespace: aws-elbv2-system
        resource: buttons
        verb: GET
