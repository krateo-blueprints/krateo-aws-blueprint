# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-emrcontainers-jobrun
  namespace: aws-emrcontainers-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-emrcontainers-jobrun-card-data
    namespace: aws-emrcontainers-system
  widgetData:
    title: Aws Emrcontainers Jobrun
    headerLeft: Provision an AWS Emrcontainers JobRun via the ACK emrcontainers controller.
    tooltip: Aws Emrcontainers Jobrun blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-emrcontainers-jobrun-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-emrcontainers-jobrun-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-emrcontainers-jobrun-install
        namespace: aws-emrcontainers-system
        resource: buttons
        verb: GET
