apiVersion: core.krateo.io/v1alpha1
kind: CompositionDefinition
metadata:
  name: aws-emrcontainers-virtualcluster
  namespace: aws-emrcontainers-system
spec:
  chart:
    # OCI artifact published by .github/workflows/release-tag.yaml.
    url: oci://ghcr.io/krateo-blueprints/charts/aws-emrcontainers-virtualcluster
    version: "0.1.1"
    # If the GHCR package is private, provide pull credentials:
    # credentials:
    #   username: braghettos
    #   passwordRef:
    #     key: token
    #     name: ghcr-credentials
    #     namespace: aws-emrcontainers-system
