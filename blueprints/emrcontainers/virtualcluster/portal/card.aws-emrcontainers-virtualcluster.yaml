# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-emrcontainers-virtualcluster
  namespace: aws-emrcontainers-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Analytics
spec:
  apiRef:
    name: aws-emrcontainers-virtualcluster-card-data
    namespace: aws-emrcontainers-system
  widgetData:
    title: Aws Emrcontainers Virtualcluster
    headerLeft: Provision an AWS Emrcontainers VirtualCluster via the ACK emrcontainers controller.
    tooltip: Aws Emrcontainers Virtualcluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Analytics
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-emrcontainers-virtualcluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Analytics", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-emrcontainers-virtualcluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-emrcontainers-virtualcluster-install
        namespace: aws-emrcontainers-system
        resource: buttons
        verb: GET
