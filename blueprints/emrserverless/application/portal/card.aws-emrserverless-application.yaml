# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-emrserverless-application
  namespace: aws-emrserverless-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-emrserverless-application-card-data
    namespace: aws-emrserverless-system
  widgetData:
    title: Aws Emrserverless Application
    headerLeft: Provision an AWS Emrserverless Application via the ACK emrserverless controller.
    tooltip: Aws Emrserverless Application blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-emrserverless-application-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-emrserverless-application-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-emrserverless-application-install
        namespace: aws-emrserverless-system
        resource: buttons
        verb: GET
