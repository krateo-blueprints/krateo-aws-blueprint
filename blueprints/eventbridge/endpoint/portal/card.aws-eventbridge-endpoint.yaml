# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eventbridge-endpoint
  namespace: aws-eventbridge-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-eventbridge-endpoint-card-data
    namespace: aws-eventbridge-system
  widgetData:
    title: Aws Eventbridge Endpoint
    headerLeft: Provision an AWS Eventbridge Endpoint via the ACK eventbridge controller.
    tooltip: Aws Eventbridge Endpoint blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eventbridge-endpoint-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eventbridge-endpoint-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eventbridge-endpoint-install
        namespace: aws-eventbridge-system
        resource: buttons
        verb: GET
