# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eventbridge-eventbus
  namespace: aws-eventbridge-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-eventbridge-eventbus-card-data
    namespace: aws-eventbridge-system
  widgetData:
    title: Aws Eventbridge Eventbus
    headerLeft: Provision an AWS Eventbridge EventBus via the ACK eventbridge controller.
    tooltip: Aws Eventbridge Eventbus blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eventbridge-eventbus-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eventbridge-eventbus-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eventbridge-eventbus-install
        namespace: aws-eventbridge-system
        resource: buttons
        verb: GET
