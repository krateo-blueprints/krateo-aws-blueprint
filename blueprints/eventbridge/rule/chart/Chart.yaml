apiVersion: v2
# Generated by tools/ackgen from the ACK Rule CRD (eventbridge.services.k8s.aws).
#
# Renders a native eventbridge.services.k8s.aws/v1alpha1 Rule resource; the ACK eventbridge
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsEventbridgeRule (plural awseventbridgerules) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Rule spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-eventbridge-rule
description: A Krateo blueprint that provisions an AWS Eventbridge Rule via the AWS Controllers for Kubernetes (ACK) eventbridge controller. Requires the ACK eventbridge controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - eventbridge
  - rule
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
