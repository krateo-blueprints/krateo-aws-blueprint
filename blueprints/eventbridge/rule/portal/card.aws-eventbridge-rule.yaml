# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-eventbridge-rule
  namespace: aws-eventbridge-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-eventbridge-rule-card-data
    namespace: aws-eventbridge-system
  widgetData:
    title: Aws Eventbridge Rule
    headerLeft: Provision an AWS Eventbridge Rule via the ACK eventbridge controller.
    tooltip: Aws Eventbridge Rule blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-eventbridge-rule-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-eventbridge-rule-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-eventbridge-rule-install
        namespace: aws-eventbridge-system
        resource: buttons
        verb: GET
