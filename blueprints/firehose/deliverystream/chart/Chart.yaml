apiVersion: v2
# Generated by tools/ackgen from the ACK DeliveryStream CRD (firehose.services.k8s.aws).
#
# Renders a native firehose.services.k8s.aws/v1alpha1 DeliveryStream resource; the ACK firehose
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsFirehoseDeliverystream (plural awsfirehosedeliverystreams) in the composition.krateo.io group.
# The Composition spec mirrors the ACK DeliveryStream spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-firehose-deliverystream
description: A Krateo blueprint that provisions an AWS Firehose DeliveryStream via the AWS Controllers for Kubernetes (ACK) firehose controller. Requires the ACK firehose controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - firehose
  - deliverystream
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
