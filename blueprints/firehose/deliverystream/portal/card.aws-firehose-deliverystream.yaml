# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-firehose-deliverystream
  namespace: aws-firehose-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-firehose-deliverystream-card-data
    namespace: aws-firehose-system
  widgetData:
    title: Aws Firehose Deliverystream
    headerLeft: Provision an AWS Firehose DeliveryStream via the ACK firehose controller.
    tooltip: Aws Firehose Deliverystream blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-firehose-deliverystream-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-firehose-deliverystream-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-firehose-deliverystream-install
        namespace: aws-firehose-system
        resource: buttons
        verb: GET
