# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-glue-job
  namespace: aws-glue-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-glue-job-card-data
    namespace: aws-glue-system
  widgetData:
    title: Aws Glue Job
    headerLeft: Provision an AWS Glue Job via the ACK glue controller.
    tooltip: Aws Glue Job blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-glue-job-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-glue-job-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-glue-job-install
        namespace: aws-glue-system
        resource: buttons
        verb: GET
