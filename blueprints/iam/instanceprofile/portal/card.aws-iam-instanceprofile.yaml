# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-iam-instanceprofile
  namespace: aws-iam-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-iam-instanceprofile-card-data
    namespace: aws-iam-system
  widgetData:
    title: Aws Iam Instanceprofile
    headerLeft: Provision an AWS Iam InstanceProfile via the ACK iam controller.
    tooltip: Aws Iam Instanceprofile blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-iam-instanceprofile-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-iam-instanceprofile-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-iam-instanceprofile-install
        namespace: aws-iam-system
        resource: buttons
        verb: GET
