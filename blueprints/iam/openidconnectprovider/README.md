# Krateo Blueprint — AWS Iam OpenIDConnectProvider

> Generated by `tools/ackgen` from the ACK `OpenIDConnectProvider` CRD (`iam.services.k8s.aws`).

Provisions an AWS Iam **OpenIDConnectProvider** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
iam controller. The chart renders a native `iam.services.k8s.aws/v1alpha1` `OpenIDConnectProvider`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsIamOpenidconnectprovider` Composition you create
becomes an AWS OpenIDConnectProvider.

## Prerequisites

- **ACK iam controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-iam-controller \
    oci://public.ecr.aws/aws-controllers-k8s/iam-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK OpenIDConnectProvider spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-iam-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsIamOpenidconnectprovider type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsIamOpenidconnectprovider` Composition type (`composition.krateo.io/v0-1-1`,
plural `awsiamopenidconnectproviders`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-iam-openidconnectprovider`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsIamOpenidconnectprovider
metadata:
  name: my-openidconnectprovider
  namespace: aws-iam-system
spec:
  region: eu-west-1
  # ... ACK OpenIDConnectProvider spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awsiamopenidconnectproviders.composition.krateo.io -n aws-iam-system
kubectl get openidconnectproviders.iam.services.k8s.aws -A
```
