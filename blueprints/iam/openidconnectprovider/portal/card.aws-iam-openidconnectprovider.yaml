# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-iam-openidconnectprovider
  namespace: aws-iam-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-iam-openidconnectprovider-card-data
    namespace: aws-iam-system
  widgetData:
    title: Aws Iam Openidconnectprovider
    headerLeft: Provision an AWS Iam OpenIDConnectProvider via the ACK iam controller.
    tooltip: Aws Iam Openidconnectprovider blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-iam-openidconnectprovider-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-iam-openidconnectprovider-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-iam-openidconnectprovider-install
        namespace: aws-iam-system
        resource: buttons
        verb: GET
