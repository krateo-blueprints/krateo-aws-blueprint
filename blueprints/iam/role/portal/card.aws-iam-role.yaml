# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-iam-role
  namespace: aws-iam-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-iam-role-card-data
    namespace: aws-iam-system
  widgetData:
    title: Aws Iam Role
    headerLeft: Provision an AWS Iam Role via the ACK iam controller.
    tooltip: Aws Iam Role blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-iam-role-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-iam-role-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-iam-role-install
        namespace: aws-iam-system
        resource: buttons
        verb: GET
