# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-iam-user
  namespace: aws-iam-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-iam-user-card-data
    namespace: aws-iam-system
  widgetData:
    title: Aws Iam User
    headerLeft: Provision an AWS Iam User via the ACK iam controller.
    tooltip: Aws Iam User blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-iam-user-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-iam-user-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-iam-user-install
        namespace: aws-iam-system
        resource: buttons
        verb: GET
