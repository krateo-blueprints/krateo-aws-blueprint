# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-kafka-cluster
  namespace: aws-kafka-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-kafka-cluster-card-data
    namespace: aws-kafka-system
  widgetData:
    title: Aws Kafka Cluster
    headerLeft: Provision an AWS Kafka Cluster via the ACK kafka controller.
    tooltip: Aws Kafka Cluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-kafka-cluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-kafka-cluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-kafka-cluster-install
        namespace: aws-kafka-system
        resource: buttons
        verb: GET
