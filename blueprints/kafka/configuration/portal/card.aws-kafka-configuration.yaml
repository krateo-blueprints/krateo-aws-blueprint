# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-kafka-configuration
  namespace: aws-kafka-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Analytics
spec:
  apiRef:
    name: aws-kafka-configuration-card-data
    namespace: aws-kafka-system
  widgetData:
    title: Aws Kafka Configuration
    headerLeft: Provision an AWS Kafka Configuration via the ACK kafka controller.
    tooltip: Aws Kafka Configuration blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Analytics
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-kafka-configuration-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Analytics", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-kafka-configuration-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-kafka-configuration-install
        namespace: aws-kafka-system
        resource: buttons
        verb: GET
