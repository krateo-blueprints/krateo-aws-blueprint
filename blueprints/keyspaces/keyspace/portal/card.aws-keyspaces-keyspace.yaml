# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-keyspaces-keyspace
  namespace: aws-keyspaces-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-keyspaces-keyspace-card-data
    namespace: aws-keyspaces-system
  widgetData:
    title: Aws Keyspaces Keyspace
    headerLeft: Provision an AWS Keyspaces Keyspace via the ACK keyspaces controller.
    tooltip: Aws Keyspaces Keyspace blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-keyspaces-keyspace-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-keyspaces-keyspace-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-keyspaces-keyspace-install
        namespace: aws-keyspaces-system
        resource: buttons
        verb: GET
