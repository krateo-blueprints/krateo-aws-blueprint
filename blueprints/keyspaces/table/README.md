# Krateo Blueprint — AWS Keyspaces Table

> Generated by `tools/ackgen` from the ACK `Table` CRD (`keyspaces.services.k8s.aws`).

Provisions an AWS Keyspaces **Table** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
keyspaces controller. The chart renders a native `keyspaces.services.k8s.aws/v1alpha1` `Table`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsKeyspacesTable` Composition you create
becomes an AWS Table.

## Prerequisites

- **ACK keyspaces controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-keyspaces-controller \
    oci://public.ecr.aws/aws-controllers-k8s/keyspaces-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK Table spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-keyspaces-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsKeyspacesTable type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsKeyspacesTable` Composition type (`composition.krateo.io/v0-1-1`,
plural `awskeyspacestables`), pulling the chart from
`oci://ghcr.io/krateo-blueprints/charts/aws-keyspaces-table`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsKeyspacesTable
metadata:
  name: my-table
  namespace: aws-keyspaces-system
spec:
  region: eu-west-1
  # ... ACK Table spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awskeyspacestables.composition.krateo.io -n aws-keyspaces-system
kubectl get tables.keyspaces.services.k8s.aws -A
```
