# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-keyspaces-table
  namespace: aws-keyspaces-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-keyspaces-table-card-data
    namespace: aws-keyspaces-system
  widgetData:
    title: Aws Keyspaces Table
    headerLeft: Provision an AWS Keyspaces Table via the ACK keyspaces controller.
    tooltip: Aws Keyspaces Table blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-keyspaces-table-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-keyspaces-table-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-keyspaces-table-install
        namespace: aws-keyspaces-system
        resource: buttons
        verb: GET
