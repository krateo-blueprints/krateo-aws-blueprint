apiVersion: v2
# Generated by tools/ackgen from the ACK Stream CRD (kinesis.services.k8s.aws).
#
# Renders a native kinesis.services.k8s.aws/v1alpha1 Stream resource; the ACK kinesis
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsKinesisStream (plural awskinesisstreams) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Stream spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-kinesis-stream
description: A Krateo blueprint that provisions an AWS Kinesis Stream via the AWS Controllers for Kubernetes (ACK) kinesis controller. Requires the ACK kinesis controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - kinesis
  - stream
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
