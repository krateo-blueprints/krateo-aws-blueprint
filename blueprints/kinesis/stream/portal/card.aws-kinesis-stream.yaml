# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-kinesis-stream
  namespace: aws-kinesis-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-kinesis-stream-card-data
    namespace: aws-kinesis-system
  widgetData:
    title: Aws Kinesis Stream
    headerLeft: Provision an AWS Kinesis Stream via the ACK kinesis controller.
    tooltip: Aws Kinesis Stream blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-kinesis-stream-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-kinesis-stream-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-kinesis-stream-install
        namespace: aws-kinesis-system
        resource: buttons
        verb: GET
