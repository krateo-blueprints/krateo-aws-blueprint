# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Form
metadata:
  name: aws-kinesis-stream-create
  namespace: aws-kinesis-system
spec:
  apiRef:
    name: aws-kinesis-stream-formdef
    namespace: aws-kinesis-system
  widgetData:
    layout: vertical
    submitActionId: submit
    propertiesToHide: []
    schema: {}
    buttonConfig:
      primary:
        label: Review & create
      secondary:
        label: Cancel
    actions:
      rest:
        - id: submit
          type: rest
          resourceRefId: create-composition
          headers:
            - 'Content-Type: application/json'
          onSuccessNavigateTo: /compositions
          successMessage: Aws Kinesis Stream composition created
          payloadToOverride:
            - name: spec
              value: ${ .json }
  widgetDataTemplate:
    - forPath: schema
      expression: ${ .schemaSpec }
  resourcesRefs:
    items:
      - id: create-composition
        apiVersion: composition.krateo.io/v0-1-1
        resource: awskinesisstreams
        namespace: aws-kinesis-system
        verb: POST
        payload:
          apiVersion: composition.krateo.io/v0-1-1
          kind: Awskinesisstream
          metadata:
            namespace: aws-kinesis-system
