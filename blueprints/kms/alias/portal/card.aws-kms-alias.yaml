# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-kms-alias
  namespace: aws-kms-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-kms-alias-card-data
    namespace: aws-kms-system
  widgetData:
    title: Aws Kms Alias
    headerLeft: Provision an AWS Kms Alias via the ACK kms controller.
    tooltip: Aws Kms Alias blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-kms-alias-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-kms-alias-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-kms-alias-install
        namespace: aws-kms-system
        resource: buttons
        verb: GET
