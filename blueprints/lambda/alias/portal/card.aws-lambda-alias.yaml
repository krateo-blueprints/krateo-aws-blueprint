# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-lambda-alias
  namespace: aws-lambda-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-lambda-alias-card-data
    namespace: aws-lambda-system
  widgetData:
    title: Aws Lambda Alias
    headerLeft: Provision an AWS Lambda Alias via the ACK lambda controller.
    tooltip: Aws Lambda Alias blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-lambda-alias-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-lambda-alias-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-lambda-alias-install
        namespace: aws-lambda-system
        resource: buttons
        verb: GET
