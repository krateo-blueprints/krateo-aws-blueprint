apiVersion: v2
# Generated by tools/ackgen from the ACK CodeSigningConfig CRD (lambda.services.k8s.aws).
#
# Renders a native lambda.services.k8s.aws/v1alpha1 CodeSigningConfig resource; the ACK lambda
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsLambdaCodesigningconfig (plural awslambdacodesigningconfigs) in the composition.krateo.io group.
# The Composition spec mirrors the ACK CodeSigningConfig spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-lambda-codesigningconfig
description: A Krateo blueprint that provisions an AWS Lambda CodeSigningConfig via the AWS Controllers for Kubernetes (ACK) lambda controller. Requires the ACK lambda controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - lambda
  - codesigningconfig
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
