# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-lambda-codesigningconfig
  namespace: aws-lambda-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-lambda-codesigningconfig-card-data
    namespace: aws-lambda-system
  widgetData:
    title: Aws Lambda Codesigningconfig
    headerLeft: Provision an AWS Lambda CodeSigningConfig via the ACK lambda controller.
    tooltip: Aws Lambda Codesigningconfig blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-lambda-codesigningconfig-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-lambda-codesigningconfig-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-lambda-codesigningconfig-install
        namespace: aws-lambda-system
        resource: buttons
        verb: GET
