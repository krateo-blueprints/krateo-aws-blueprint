# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-lambda-functionurlconfig
  namespace: aws-lambda-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-lambda-functionurlconfig-card-data
    namespace: aws-lambda-system
  widgetData:
    title: Aws Lambda Functionurlconfig
    headerLeft: Provision an AWS Lambda FunctionURLConfig via the ACK lambda controller.
    tooltip: Aws Lambda Functionurlconfig blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-lambda-functionurlconfig-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-lambda-functionurlconfig-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-lambda-functionurlconfig-install
        namespace: aws-lambda-system
        resource: buttons
        verb: GET
