# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-lambda-layerversion
  namespace: aws-lambda-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-lambda-layerversion-card-data
    namespace: aws-lambda-system
  widgetData:
    title: Aws Lambda Layerversion
    headerLeft: Provision an AWS Lambda LayerVersion via the ACK lambda controller.
    tooltip: Aws Lambda Layerversion blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-lambda-layerversion-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-lambda-layerversion-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-lambda-layerversion-install
        namespace: aws-lambda-system
        resource: buttons
        verb: GET
