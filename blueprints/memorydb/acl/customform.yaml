apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-memorydb-acl-card
  namespace: aws-memorydb-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-memorydb-acl-card
  namespace: aws-memorydb-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-memorydb-acl-customform
      namespace: aws-memorydb-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-memorydb-acl
      namespace: aws-memorydb-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Memorydb ACL via the ACK memorydb controller.
      icon: fa-brands fa-aws
      tags: aws-memorydb-system,0.1.1
      title: aws-memorydb-acl
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-memorydb-acl-card
    namespace: aws-memorydb-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-memorydb-system/compositiondefinitions/aws-memorydb-acl"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-memorydb-acl-customform
  namespace: aws-memorydb-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-memorydb-acl-customform
  namespace: aws-memorydb-system
spec:
  type: customform
  propsRef:
    name: template-aws-memorydb-acl-customform
    namespace: aws-memorydb-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-1
        verb: POST
        resource: awsmemorydbacls
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-memorydb-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-1") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsmemorydbacls.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
