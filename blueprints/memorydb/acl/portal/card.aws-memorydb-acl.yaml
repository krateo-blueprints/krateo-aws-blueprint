# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-memorydb-acl
  namespace: aws-memorydb-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-memorydb-acl-card-data
    namespace: aws-memorydb-system
  widgetData:
    title: Aws Memorydb Acl
    headerLeft: Provision an AWS Memorydb ACL via the ACK memorydb controller.
    tooltip: Aws Memorydb Acl blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-memorydb-acl-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-memorydb-acl-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-memorydb-acl-install
        namespace: aws-memorydb-system
        resource: buttons
        verb: GET
