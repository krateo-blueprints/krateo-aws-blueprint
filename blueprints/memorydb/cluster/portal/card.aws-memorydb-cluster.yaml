# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-memorydb-cluster
  namespace: aws-memorydb-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-memorydb-cluster-card-data
    namespace: aws-memorydb-system
  widgetData:
    title: Aws Memorydb Cluster
    headerLeft: Provision an AWS Memorydb Cluster via the ACK memorydb controller.
    tooltip: Aws Memorydb Cluster blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-memorydb-cluster-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-memorydb-cluster-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-memorydb-cluster-install
        namespace: aws-memorydb-system
        resource: buttons
        verb: GET
