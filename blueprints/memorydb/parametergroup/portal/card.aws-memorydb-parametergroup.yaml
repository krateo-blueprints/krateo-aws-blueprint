# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-memorydb-parametergroup
  namespace: aws-memorydb-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-memorydb-parametergroup-card-data
    namespace: aws-memorydb-system
  widgetData:
    title: Aws Memorydb Parametergroup
    headerLeft: Provision an AWS Memorydb ParameterGroup via the ACK memorydb controller.
    tooltip: Aws Memorydb Parametergroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-memorydb-parametergroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-memorydb-parametergroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-memorydb-parametergroup-install
        namespace: aws-memorydb-system
        resource: buttons
        verb: GET
