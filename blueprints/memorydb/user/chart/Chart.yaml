apiVersion: v2
# Generated by tools/ackgen from the ACK User CRD (memorydb.services.k8s.aws).
#
# Renders a native memorydb.services.k8s.aws/v1alpha1 User resource; the ACK memorydb
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsMemorydbUser (plural awsmemorydbusers) in the composition.krateo.io group.
# The Composition spec mirrors the ACK User spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-memorydb-user
description: A Krateo blueprint that provisions an AWS Memorydb User via the AWS Controllers for Kubernetes (ACK) memorydb controller. Requires the ACK memorydb controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - memorydb
  - user
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "database"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
