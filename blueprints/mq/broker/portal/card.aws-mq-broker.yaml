# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-mq-broker
  namespace: aws-mq-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-mq-broker-card-data
    namespace: aws-mq-system
  widgetData:
    title: Aws Mq Broker
    headerLeft: Provision an AWS Mq Broker via the ACK mq controller.
    tooltip: Aws Mq Broker blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-mq-broker-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-mq-broker-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-mq-broker-install
        namespace: aws-mq-system
        resource: buttons
        verb: GET
