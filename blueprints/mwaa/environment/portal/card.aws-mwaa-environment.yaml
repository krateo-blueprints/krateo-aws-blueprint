# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-mwaa-environment
  namespace: aws-mwaa-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-mwaa-environment-card-data
    namespace: aws-mwaa-system
  widgetData:
    title: Aws Mwaa Environment
    headerLeft: Provision an AWS Mwaa Environment via the ACK mwaa controller.
    tooltip: Aws Mwaa Environment blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-mwaa-environment-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-mwaa-environment-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-mwaa-environment-install
        namespace: aws-mwaa-system
        resource: buttons
        verb: GET
