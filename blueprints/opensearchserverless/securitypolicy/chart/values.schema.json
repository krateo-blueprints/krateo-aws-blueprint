{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Provision an AWS Opensearchserverless SecurityPolicy via the ACK opensearchserverless controller. Fields mirror the opensearchserverless.services.k8s.aws/v1alpha1 SecurityPolicy spec.",
  "properties": {
    "description": {
      "description": "A description of the policy. Typically used to store information about the\npermissions defined in the policy.",
      "type": "string"
    },
    "name": {
      "description": "The name of the policy.\n\nRegex Pattern: `^[a-z][a-z0-9-]+$`",
      "type": "string"
    },
    "policy": {
      "description": "The JSON policy document to use as the content for the new policy.\n\nRegex Pattern: `[\\u0009\\u000A\\u000D\\u0020-\\u007E\\u00A1-\\u00FF]+`",
      "type": "string"
    },
    "region": {
      "default": "",
      "description": "AWS region for this resource (e.g. eu-west-1). Rendered as the services.k8s.aws/region annotation. Empty inherits the ACK controller's default region.",
      "title": "AWS region",
      "type": "string"
    },
    "type": {
      "description": "The type of security policy.",
      "type": "string"
    }
  },
  "required": [
    "name",
    "policy",
    "type"
  ],
  "title": "AWS Opensearchserverless SecurityPolicy",
  "type": "object"
}
