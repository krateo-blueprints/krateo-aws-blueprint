# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-opensearchservice-domain
  namespace: aws-opensearchservice-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-opensearchservice-domain-card-data
    namespace: aws-opensearchservice-system
  widgetData:
    title: Aws Opensearchservice Domain
    headerLeft: Provision an AWS Opensearchservice Domain via the ACK opensearchservice controller.
    tooltip: Aws Opensearchservice Domain blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-opensearchservice-domain-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-opensearchservice-domain-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-opensearchservice-domain-install
        namespace: aws-opensearchservice-system
        resource: buttons
        verb: GET
