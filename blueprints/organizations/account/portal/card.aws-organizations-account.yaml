# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-organizations-account
  namespace: aws-organizations-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-organizations-account-card-data
    namespace: aws-organizations-system
  widgetData:
    title: Aws Organizations Account
    headerLeft: Provision an AWS Organizations Account via the ACK organizations controller.
    tooltip: Aws Organizations Account blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-organizations-account-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-organizations-account-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-organizations-account-install
        namespace: aws-organizations-system
        resource: buttons
        verb: GET
