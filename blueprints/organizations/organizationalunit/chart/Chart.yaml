apiVersion: v2
# Generated by tools/ackgen from the ACK OrganizationalUnit CRD (organizations.services.k8s.aws).
#
# Renders a native organizations.services.k8s.aws/v1alpha1 OrganizationalUnit resource; the ACK organizations
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsOrganizationsOrganizationalunit (plural awsorganizationsorganizationalunits) in the composition.krateo.io group.
# The Composition spec mirrors the ACK OrganizationalUnit spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-organizations-organizationalunit
description: A Krateo blueprint that provisions an AWS Organizations OrganizationalUnit via the AWS Controllers for Kubernetes (ACK) organizations controller. Requires the ACK organizations controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - organizations
  - organizationalunit
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
