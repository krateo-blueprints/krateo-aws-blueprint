# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-organizations-organizationalunit
  namespace: aws-organizations-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-organizations-organizationalunit-card-data
    namespace: aws-organizations-system
  widgetData:
    title: Aws Organizations Organizationalunit
    headerLeft: Provision an AWS Organizations OrganizationalUnit via the ACK organizations controller.
    tooltip: Aws Organizations Organizationalunit blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-organizations-organizationalunit-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-organizations-organizationalunit-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-organizations-organizationalunit-install
        namespace: aws-organizations-system
        resource: buttons
        verb: GET
