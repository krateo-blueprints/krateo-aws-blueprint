# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-pipes-pipe
  namespace: aws-pipes-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-pipes-pipe-card-data
    namespace: aws-pipes-system
  widgetData:
    title: Aws Pipes Pipe
    headerLeft: Provision an AWS Pipes Pipe via the ACK pipes controller.
    tooltip: Aws Pipes Pipe blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-pipes-pipe-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-pipes-pipe-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-pipes-pipe-install
        namespace: aws-pipes-system
        resource: buttons
        verb: GET
