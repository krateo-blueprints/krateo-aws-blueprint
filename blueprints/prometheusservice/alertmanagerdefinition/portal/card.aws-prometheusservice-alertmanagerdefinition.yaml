# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-prometheusservice-alertmanagerdefinition
  namespace: aws-prometheusservice-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-prometheusservice-alertmanagerdefinition-card-data
    namespace: aws-prometheusservice-system
  widgetData:
    title: Aws Prometheusservice Alertmanagerdefinition
    headerLeft: Provision an AWS Prometheusservice AlertManagerDefinition via the ACK prometheusservice controller.
    tooltip: Aws Prometheusservice Alertmanagerdefinition blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-prometheusservice-alertmanagerdefinition-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-prometheusservice-alertmanagerdefinition-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-prometheusservice-alertmanagerdefinition-install
        namespace: aws-prometheusservice-system
        resource: buttons
        verb: GET
