# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-prometheusservice-loggingconfiguration
  namespace: aws-prometheusservice-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-prometheusservice-loggingconfiguration-card-data
    namespace: aws-prometheusservice-system
  widgetData:
    title: Aws Prometheusservice Loggingconfiguration
    headerLeft: Provision an AWS Prometheusservice LoggingConfiguration via the ACK prometheusservice controller.
    tooltip: Aws Prometheusservice Loggingconfiguration blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-prometheusservice-loggingconfiguration-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-prometheusservice-loggingconfiguration-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-prometheusservice-loggingconfiguration-install
        namespace: aws-prometheusservice-system
        resource: buttons
        verb: GET
