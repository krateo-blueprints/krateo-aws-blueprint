apiVersion: v2
# Generated by tools/ackgen from the ACK RuleGroupsNamespace CRD (prometheusservice.services.k8s.aws).
#
# Renders a native prometheusservice.services.k8s.aws/v1alpha1 RuleGroupsNamespace resource; the ACK prometheusservice
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsPrometheusserviceRulegroupsnamespace (plural awsprometheusservicerulegroupsnamespaces) in the composition.krateo.io group.
# The Composition spec mirrors the ACK RuleGroupsNamespace spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-prometheusservice-rulegroupsnamespace
description: A Krateo blueprint that provisions an AWS Prometheusservice RuleGroupsNamespace via the AWS Controllers for Kubernetes (ACK) prometheusservice controller. Requires the ACK prometheusservice controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - prometheusservice
  - rulegroupsnamespace
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "observability"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
