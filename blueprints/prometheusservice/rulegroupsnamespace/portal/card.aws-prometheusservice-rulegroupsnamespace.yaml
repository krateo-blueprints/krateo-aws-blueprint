# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-prometheusservice-rulegroupsnamespace
  namespace: aws-prometheusservice-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-prometheusservice-rulegroupsnamespace-card-data
    namespace: aws-prometheusservice-system
  widgetData:
    title: Aws Prometheusservice Rulegroupsnamespace
    headerLeft: Provision an AWS Prometheusservice RuleGroupsNamespace via the ACK prometheusservice controller.
    tooltip: Aws Prometheusservice Rulegroupsnamespace blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-prometheusservice-rulegroupsnamespace-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-prometheusservice-rulegroupsnamespace-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-prometheusservice-rulegroupsnamespace-install
        namespace: aws-prometheusservice-system
        resource: buttons
        verb: GET
