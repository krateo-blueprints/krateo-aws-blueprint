apiVersion: v2
# Generated by tools/ackgen from the ACK Analysis CRD (quicksight.services.k8s.aws).
#
# Renders a native quicksight.services.k8s.aws/v1alpha1 Analysis resource; the ACK quicksight
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsQuicksightAnalysis (plural awsquicksightanalyses) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Analysis spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-quicksight-analysis
description: A Krateo blueprint that provisions an AWS Quicksight Analysis via the AWS Controllers for Kubernetes (ACK) quicksight controller. Requires the ACK quicksight controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - quicksight
  - analysis
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "observability"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
