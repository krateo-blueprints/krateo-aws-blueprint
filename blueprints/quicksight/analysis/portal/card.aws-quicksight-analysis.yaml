# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-quicksight-analysis
  namespace: aws-quicksight-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-quicksight-analysis-card-data
    namespace: aws-quicksight-system
  widgetData:
    title: Aws Quicksight Analysis
    headerLeft: Provision an AWS Quicksight Analysis via the ACK quicksight controller.
    tooltip: Aws Quicksight Analysis blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-quicksight-analysis-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-quicksight-analysis-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-quicksight-analysis-install
        namespace: aws-quicksight-system
        resource: buttons
        verb: GET
