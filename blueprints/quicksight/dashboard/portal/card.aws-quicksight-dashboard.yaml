# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-quicksight-dashboard
  namespace: aws-quicksight-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-quicksight-dashboard-card-data
    namespace: aws-quicksight-system
  widgetData:
    title: Aws Quicksight Dashboard
    headerLeft: Provision an AWS Quicksight Dashboard via the ACK quicksight controller.
    tooltip: Aws Quicksight Dashboard blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-quicksight-dashboard-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-quicksight-dashboard-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-quicksight-dashboard-install
        namespace: aws-quicksight-system
        resource: buttons
        verb: GET
