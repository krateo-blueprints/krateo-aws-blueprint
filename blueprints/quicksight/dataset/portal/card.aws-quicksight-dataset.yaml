# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-quicksight-dataset
  namespace: aws-quicksight-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Analytics
spec:
  apiRef:
    name: aws-quicksight-dataset-card-data
    namespace: aws-quicksight-system
  widgetData:
    title: Aws Quicksight Dataset
    headerLeft: Provision an AWS Quicksight DataSet via the ACK quicksight controller.
    tooltip: Aws Quicksight Dataset blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Analytics
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-quicksight-dataset-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Analytics", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-quicksight-dataset-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-quicksight-dataset-install
        namespace: aws-quicksight-system
        resource: buttons
        verb: GET
