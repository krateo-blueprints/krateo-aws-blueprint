# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ram-permission
  namespace: aws-ram-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-ram-permission-card-data
    namespace: aws-ram-system
  widgetData:
    title: Aws Ram Permission
    headerLeft: Provision an AWS Ram Permission via the ACK ram controller.
    tooltip: Aws Ram Permission blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ram-permission-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ram-permission-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ram-permission-install
        namespace: aws-ram-system
        resource: buttons
        verb: GET
