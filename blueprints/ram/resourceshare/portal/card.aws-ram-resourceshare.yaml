# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ram-resourceshare
  namespace: aws-ram-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-ram-resourceshare-card-data
    namespace: aws-ram-system
  widgetData:
    title: Aws Ram Resourceshare
    headerLeft: Provision an AWS Ram ResourceShare via the ACK ram controller.
    tooltip: Aws Ram Resourceshare blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ram-resourceshare-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ram-resourceshare-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ram-resourceshare-install
        namespace: aws-ram-system
        resource: buttons
        verb: GET
