# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbclusterendpoint
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-rds-dbclusterendpoint-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbclusterendpoint
    headerLeft: Provision an AWS Rds DBClusterEndpoint via the ACK rds controller.
    tooltip: Aws Rds Dbclusterendpoint blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbclusterendpoint-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbclusterendpoint-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbclusterendpoint-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
