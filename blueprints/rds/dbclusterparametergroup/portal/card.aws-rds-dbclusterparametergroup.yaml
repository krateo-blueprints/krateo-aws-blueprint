# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbclusterparametergroup
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-rds-dbclusterparametergroup-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbclusterparametergroup
    headerLeft: Provision an AWS Rds DBClusterParameterGroup via the ACK rds controller.
    tooltip: Aws Rds Dbclusterparametergroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbclusterparametergroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbclusterparametergroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbclusterparametergroup-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
