# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbclustersnapshot
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-rds-dbclustersnapshot-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbclustersnapshot
    headerLeft: Provision an AWS Rds DBClusterSnapshot via the ACK rds controller.
    tooltip: Aws Rds Dbclustersnapshot blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbclustersnapshot-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbclustersnapshot-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbclustersnapshot-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
