# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbinstance
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-rds-dbinstance-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbinstance
    headerLeft: Provision an AWS Rds DBInstance via the ACK rds controller.
    tooltip: Aws Rds Dbinstance blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbinstance-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbinstance-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbinstance-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
