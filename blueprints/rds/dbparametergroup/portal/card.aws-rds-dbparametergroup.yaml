# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbparametergroup
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-rds-dbparametergroup-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbparametergroup
    headerLeft: Provision an AWS Rds DBParameterGroup via the ACK rds controller.
    tooltip: Aws Rds Dbparametergroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbparametergroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbparametergroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbparametergroup-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
