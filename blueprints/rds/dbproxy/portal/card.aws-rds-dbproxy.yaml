# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbproxy
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Database
spec:
  apiRef:
    name: aws-rds-dbproxy-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbproxy
    headerLeft: Provision an AWS Rds DBProxy via the ACK rds controller.
    tooltip: Aws Rds Dbproxy blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Database
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbproxy-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Database", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbproxy-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbproxy-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
