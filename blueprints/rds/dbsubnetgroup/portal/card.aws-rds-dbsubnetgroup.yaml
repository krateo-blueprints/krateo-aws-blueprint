# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-rds-dbsubnetgroup
  namespace: aws-rds-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-rds-dbsubnetgroup-card-data
    namespace: aws-rds-system
  widgetData:
    title: Aws Rds Dbsubnetgroup
    headerLeft: Provision an AWS Rds DBSubnetGroup via the ACK rds controller.
    tooltip: Aws Rds Dbsubnetgroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-rds-dbsubnetgroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-rds-dbsubnetgroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-rds-dbsubnetgroup-install
        namespace: aws-rds-system
        resource: buttons
        verb: GET
