# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-recyclebin-rule
  namespace: aws-recyclebin-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-recyclebin-rule-card-data
    namespace: aws-recyclebin-system
  widgetData:
    title: Aws Recyclebin Rule
    headerLeft: Provision an AWS Recyclebin Rule via the ACK recyclebin controller.
    tooltip: Aws Recyclebin Rule blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-recyclebin-rule-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-recyclebin-rule-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-recyclebin-rule-install
        namespace: aws-recyclebin-system
        resource: buttons
        verb: GET
