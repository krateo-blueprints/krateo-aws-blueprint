apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-route53-healthcheck-card
  namespace: aws-route53-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-route53-healthcheck-card
  namespace: aws-route53-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-route53-healthcheck-customform
      namespace: aws-route53-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-route53-healthcheck
      namespace: aws-route53-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Route53 HealthCheck via the ACK route53 controller.
      icon: fa-brands fa-aws
      tags: aws-route53-system,0.1.0
      title: aws-route53-healthcheck
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-route53-healthcheck-card
    namespace: aws-route53-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-route53-system/compositiondefinitions/aws-route53-healthcheck"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-route53-healthcheck-customform
  namespace: aws-route53-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-route53-healthcheck-customform
  namespace: aws-route53-system
spec:
  type: customform
  propsRef:
    name: template-aws-route53-healthcheck-customform
    namespace: aws-route53-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-0
        verb: POST
        resource: awsroute53healthchecks
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-route53-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-0") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsroute53healthchecks.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
