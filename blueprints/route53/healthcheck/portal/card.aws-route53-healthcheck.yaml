# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-route53-healthcheck
  namespace: aws-route53-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Networking
spec:
  apiRef:
    name: aws-route53-healthcheck-card-data
    namespace: aws-route53-system
  widgetData:
    title: Aws Route53 Healthcheck
    headerLeft: Provision an AWS Route53 HealthCheck via the ACK route53 controller.
    tooltip: Aws Route53 Healthcheck blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Networking
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-route53-healthcheck-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Networking", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-route53-healthcheck-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-route53-healthcheck-install
        namespace: aws-route53-system
        resource: buttons
        verb: GET
