# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-route53-hostedzone
  namespace: aws-route53-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-route53-hostedzone-card-data
    namespace: aws-route53-system
  widgetData:
    title: Aws Route53 Hostedzone
    headerLeft: Provision an AWS Route53 HostedZone via the ACK route53 controller.
    tooltip: Aws Route53 Hostedzone blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-route53-hostedzone-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-route53-hostedzone-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-route53-hostedzone-install
        namespace: aws-route53-system
        resource: buttons
        verb: GET
