apiVersion: v2
# Generated by tools/ackgen from the ACK ResolverRule CRD (route53resolver.services.k8s.aws).
#
# Renders a native route53resolver.services.k8s.aws/v1alpha1 ResolverRule resource; the ACK route53resolver
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsRoute53resolverResolverrule (plural awsroute53resolverresolverrules) in the composition.krateo.io group.
# The Composition spec mirrors the ACK ResolverRule spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-route53resolver-resolverrule
description: A Krateo blueprint that provisions an AWS Route53resolver ResolverRule via the AWS Controllers for Kubernetes (ACK) route53resolver controller. Requires the ACK route53resolver controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - route53resolver
  - resolverrule
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "network"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
