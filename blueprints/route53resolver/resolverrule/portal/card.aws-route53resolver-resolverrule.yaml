# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-route53resolver-resolverrule
  namespace: aws-route53resolver-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-route53resolver-resolverrule-card-data
    namespace: aws-route53resolver-system
  widgetData:
    title: Aws Route53resolver Resolverrule
    headerLeft: Provision an AWS Route53resolver ResolverRule via the ACK route53resolver controller.
    tooltip: Aws Route53resolver Resolverrule blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-route53resolver-resolverrule-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-route53resolver-resolverrule-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-route53resolver-resolverrule-install
        namespace: aws-route53resolver-system
        resource: buttons
        verb: GET
