apiVersion: v1
data:
  drawer: "true"
  form: "true"
  showFormStructure: "true"
  drawerSize: large
kind: ConfigMap
metadata:
  name: template-aws-route53resolver-resolverruleassociation-card
  namespace: aws-route53resolver-system
---
apiVersion: templates.krateo.io/v1alpha1
kind: Widget
metadata:
  name: template-aws-route53resolver-resolverruleassociation-card
  namespace: aws-route53resolver-system
spec:
  actions:
  - template:
      apiVersion: templates.krateo.io/v1alpha1
      name: template-aws-route53resolver-resolverruleassociation-customform
      namespace: aws-route53resolver-system
      resource: customforms
      verb: GET
  - template:
      apiVersion: core.krateo.io/v1alpha1
      name: aws-route53resolver-resolverruleassociation
      namespace: aws-route53resolver-system
      resource: compositiondefinitions
      verb: DELETE
  app:
    template:
      color: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | if .status == "False" then "orange" elif .status == "True" then "green" else "grey" end }
      content: Provision an AWS Route53resolver ResolverRuleAssociation via the ACK route53resolver controller.
      icon: fa-brands fa-aws
      tags: aws-route53resolver-system,0.1.0
      title: aws-route53resolver-resolverruleassociation
      status: ${ .getCompositionDefinition.status.conditions[] | select(.type == "Ready") | "Ready:" + .status }
      date: ${ .getCompositionDefinition.metadata.creationTimestamp }
  propsRef:
    name: template-aws-route53resolver-resolverruleassociation-card
    namespace: aws-route53resolver-system
  type: card
  api:
  - name: getCompositionDefinition
    path: "/apis/core.krateo.io/v1alpha1/namespaces/aws-route53resolver-system/compositiondefinitions/aws-route53resolver-resolverruleassociation"
    verb: GET
    headers:
    - 'Accept: application/json'
---
kind: ConfigMap
apiVersion: v1
metadata:
  name: template-aws-route53resolver-resolverruleassociation-customform
  namespace: aws-route53resolver-system
data:
  onSubmitId: submit
  onSubmitVerb: POST
  payloadFormKey: spec
  redirectRoute: /compositions/${metadata.namespace}/${metadata.name}
  redirectTimeout: "50"
---
apiVersion: templates.krateo.io/v1alpha1
kind: CustomForm
metadata:
  name: template-aws-route53resolver-resolverruleassociation-customform
  namespace: aws-route53resolver-system
spec:
  type: customform
  propsRef:
    name: template-aws-route53resolver-resolverruleassociation-customform
    namespace: aws-route53resolver-system
  actions:
    - template:
        id: submit
        apiVersion: composition.krateo.io/v0-1-0
        verb: POST
        resource: awsroute53resolverresolverruleassociations
        payloadToOverride:
        - name: metadata.namespace
          value: ${ "aws-route53resolver-system" }
  app:
    template:
      schema: ${ .getCRD | .spec.versions[] | select(.name == "v0-1-0") | .schema.openAPIV3Schema.properties.spec }
  api:
  - name: getCRD
    path: "/apis/apiextensions.k8s.io/v1/customresourcedefinitions/awsroute53resolverresolverruleassociations.composition.krateo.io"
    verb: GET
    headers:
    - 'Accept: application/json'
