# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-route53resolver-resolverruleassociation
  namespace: aws-route53resolver-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Networking
spec:
  apiRef:
    name: aws-route53resolver-resolverruleassociation-card-data
    namespace: aws-route53resolver-system
  widgetData:
    title: Aws Route53resolver Resolverruleassociation
    headerLeft: Provision an AWS Route53resolver ResolverRuleAssociation via the ACK route53resolver controller.
    tooltip: Aws Route53resolver Resolverruleassociation blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Networking
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-route53resolver-resolverruleassociation-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Networking", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-route53resolver-resolverruleassociation-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-route53resolver-resolverruleassociation-install
        namespace: aws-route53resolver-system
        resource: buttons
        verb: GET
