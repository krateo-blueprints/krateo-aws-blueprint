# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-s3-bucket
  namespace: aws-s3-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-s3-bucket-card-data
    namespace: aws-s3-system
  widgetData:
    title: Aws S3 Bucket
    headerLeft: Provision an AWS S3 Bucket via the ACK s3 controller.
    tooltip: Aws S3 Bucket blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-s3-bucket-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-s3-bucket-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-s3-bucket-install
        namespace: aws-s3-system
        resource: buttons
        verb: GET
