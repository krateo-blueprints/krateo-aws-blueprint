apiVersion: v2
# Generated by tools/ackgen from the ACK AccessPoint CRD (s3control.services.k8s.aws).
#
# Renders a native s3control.services.k8s.aws/v1alpha1 AccessPoint resource; the ACK s3control
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsS3controlAccesspoint (plural awss3controlaccesspoints) in the composition.krateo.io group.
# The Composition spec mirrors the ACK AccessPoint spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-s3control-accesspoint
description: A Krateo blueprint that provisions an AWS S3control AccessPoint via the AWS Controllers for Kubernetes (ACK) s3control controller. Requires the ACK s3control controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - s3control
  - accesspoint
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
