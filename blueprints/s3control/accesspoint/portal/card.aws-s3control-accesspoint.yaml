# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-s3control-accesspoint
  namespace: aws-s3control-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-s3control-accesspoint-card-data
    namespace: aws-s3control-system
  widgetData:
    title: Aws S3control Accesspoint
    headerLeft: Provision an AWS S3control AccessPoint via the ACK s3control controller.
    tooltip: Aws S3control Accesspoint blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-s3control-accesspoint-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-s3control-accesspoint-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-s3control-accesspoint-install
        namespace: aws-s3control-system
        resource: buttons
        verb: GET
