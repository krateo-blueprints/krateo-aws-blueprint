# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Form
metadata:
  name: aws-s3control-accesspoint-create
  namespace: aws-s3control-system
spec:
  apiRef:
    name: aws-s3control-accesspoint-formdef
    namespace: aws-s3control-system
  widgetData:
    layout: vertical
    submitActionId: submit
    propertiesToHide: []
    schema: {}
    buttonConfig:
      primary:
        label: Review & create
      secondary:
        label: Cancel
    actions:
      rest:
        - id: submit
          type: rest
          resourceRefId: create-composition
          headers:
            - 'Content-Type: application/json'
          onSuccessNavigateTo: /compositions
          successMessage: Aws S3control Accesspoint composition created
          payloadToOverride:
            - name: spec
              value: ${ .json }
  widgetDataTemplate:
    - forPath: schema
      expression: ${ .schemaSpec }
  resourcesRefs:
    items:
      - id: create-composition
        apiVersion: composition.krateo.io/v0-1-1
        resource: awss3controlaccesspoints
        namespace: aws-s3control-system
        verb: POST
        payload:
          apiVersion: composition.krateo.io/v0-1-1
          kind: Awss3controlaccesspoint
          metadata:
            namespace: aws-s3control-system
