# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-s3files-filesystem
  namespace: aws-s3files-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-s3files-filesystem-card-data
    namespace: aws-s3files-system
  widgetData:
    title: Aws S3files Filesystem
    headerLeft: Provision an AWS S3files FileSystem via the ACK s3files controller.
    tooltip: Aws S3files Filesystem blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-s3files-filesystem-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-s3files-filesystem-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-s3files-filesystem-install
        namespace: aws-s3files-system
        resource: buttons
        verb: GET
