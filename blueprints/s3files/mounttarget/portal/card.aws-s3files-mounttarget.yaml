# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-s3files-mounttarget
  namespace: aws-s3files-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Storage
spec:
  apiRef:
    name: aws-s3files-mounttarget-card-data
    namespace: aws-s3files-system
  widgetData:
    title: Aws S3files Mounttarget
    headerLeft: Provision an AWS S3files MountTarget via the ACK s3files controller.
    tooltip: Aws S3files Mounttarget blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Storage
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-s3files-mounttarget-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Storage", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-s3files-mounttarget-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-s3files-mounttarget-install
        namespace: aws-s3files-system
        resource: buttons
        verb: GET
