# Krateo Blueprint — AWS Sagemaker App

> Generated by `tools/ackgen` from the ACK `App` CRD (`sagemaker.services.k8s.aws`).

Provisions an AWS Sagemaker **App** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
sagemaker controller. The chart renders a native `sagemaker.services.k8s.aws/v1alpha1` `App`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsSagemakerApp` Composition you create
becomes an AWS App.

## Prerequisites

- **ACK sagemaker controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-sagemaker-controller \
    oci://public.ecr.aws/aws-controllers-k8s/sagemaker-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK App spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-sagemaker-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsSagemakerApp type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsSagemakerApp` Composition type (`composition.krateo.io/v0-1-1`,
plural `awssagemakerapps`), pulling the chart from
`oci://ghcr.io/krateo-blueprints/charts/aws-sagemaker-app`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-1
kind: AwsSagemakerApp
metadata:
  name: my-app
  namespace: aws-sagemaker-system
spec:
  region: eu-west-1
  # ... ACK App spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awssagemakerapps.composition.krateo.io -n aws-sagemaker-system
kubectl get apps.sagemaker.services.k8s.aws -A
```
