# Krateo Blueprint — AWS Sagemaker Domain

> Generated by `tools/ackgen` from the ACK `Domain` CRD (`sagemaker.services.k8s.aws`).

Provisions an AWS Sagemaker **Domain** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
sagemaker controller. The chart renders a native `sagemaker.services.k8s.aws/v1alpha1` `Domain`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsSagemakerDomain` Composition you create
becomes an AWS Domain.

## Prerequisites

- **ACK sagemaker controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-sagemaker-controller \
    oci://public.ecr.aws/aws-controllers-k8s/sagemaker-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK Domain spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-sagemaker-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsSagemakerDomain type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsSagemakerDomain` Composition type (`composition.krateo.io/v0-1-0`,
plural `awssagemakerdomains`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-sagemaker-domain`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsSagemakerDomain
metadata:
  name: my-domain
  namespace: aws-sagemaker-system
spec:
  region: eu-west-1
  # ... ACK Domain spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awssagemakerdomains.composition.krateo.io -n aws-sagemaker-system
kubectl get domains.sagemaker.services.k8s.aws -A
```
