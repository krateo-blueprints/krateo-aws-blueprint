# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sagemaker-featuregroup
  namespace: aws-sagemaker-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sagemaker-featuregroup-card-data
    namespace: aws-sagemaker-system
  widgetData:
    title: Aws Sagemaker Featuregroup
    headerLeft: Provision an AWS Sagemaker FeatureGroup via the ACK sagemaker controller.
    tooltip: Aws Sagemaker Featuregroup blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sagemaker-featuregroup-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sagemaker-featuregroup-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sagemaker-featuregroup-install
        namespace: aws-sagemaker-system
        resource: buttons
        verb: GET
