# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sagemaker-inferencecomponent
  namespace: aws-sagemaker-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sagemaker-inferencecomponent-card-data
    namespace: aws-sagemaker-system
  widgetData:
    title: Aws Sagemaker Inferencecomponent
    headerLeft: Provision an AWS Sagemaker InferenceComponent via the ACK sagemaker controller.
    tooltip: Aws Sagemaker Inferencecomponent blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sagemaker-inferencecomponent-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sagemaker-inferencecomponent-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sagemaker-inferencecomponent-install
        namespace: aws-sagemaker-system
        resource: buttons
        verb: GET
