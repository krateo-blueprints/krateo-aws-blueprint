# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sagemaker-labelingjob
  namespace: aws-sagemaker-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sagemaker-labelingjob-card-data
    namespace: aws-sagemaker-system
  widgetData:
    title: Aws Sagemaker Labelingjob
    headerLeft: Provision an AWS Sagemaker LabelingJob via the ACK sagemaker controller.
    tooltip: Aws Sagemaker Labelingjob blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sagemaker-labelingjob-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sagemaker-labelingjob-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sagemaker-labelingjob-install
        namespace: aws-sagemaker-system
        resource: buttons
        verb: GET
