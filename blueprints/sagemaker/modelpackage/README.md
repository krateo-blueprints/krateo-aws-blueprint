# Krateo Blueprint — AWS Sagemaker ModelPackage

> Generated by `tools/ackgen` from the ACK `ModelPackage` CRD (`sagemaker.services.k8s.aws`).

Provisions an AWS Sagemaker **ModelPackage** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
sagemaker controller. The chart renders a native `sagemaker.services.k8s.aws/v1alpha1` `ModelPackage`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsSagemakerModelpackage` Composition you create
becomes an AWS ModelPackage.

## Prerequisites

- **ACK sagemaker controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-sagemaker-controller \
    oci://public.ecr.aws/aws-controllers-k8s/sagemaker-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK ModelPackage spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-sagemaker-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsSagemakerModelpackage type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsSagemakerModelpackage` Composition type (`composition.krateo.io/v0-1-0`,
plural `awssagemakermodelpackages`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-sagemaker-modelpackage`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsSagemakerModelpackage
metadata:
  name: my-modelpackage
  namespace: aws-sagemaker-system
spec:
  region: eu-west-1
  # ... ACK ModelPackage spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awssagemakermodelpackages.composition.krateo.io -n aws-sagemaker-system
kubectl get modelpackages.sagemaker.services.k8s.aws -A
```
