apiVersion: v2
# Generated by tools/ackgen from the ACK ModelPackageGroup CRD (sagemaker.services.k8s.aws).
#
# Renders a native sagemaker.services.k8s.aws/v1alpha1 ModelPackageGroup resource; the ACK sagemaker
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsSagemakerModelpackagegroup (plural awssagemakermodelpackagegroups) in the composition.krateo.io group.
# The Composition spec mirrors the ACK ModelPackageGroup spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-sagemaker-modelpackagegroup
description: A Krateo blueprint that provisions an AWS Sagemaker ModelPackageGroup via the AWS Controllers for Kubernetes (ACK) sagemaker controller. Requires the ACK sagemaker controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - sagemaker
  - modelpackagegroup
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateo.io/category: "compute"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
