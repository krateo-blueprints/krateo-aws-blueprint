# Krateo Blueprint — AWS Sagemaker MonitoringSchedule

> Generated by `tools/ackgen` from the ACK `MonitoringSchedule` CRD (`sagemaker.services.k8s.aws`).

Provisions an AWS Sagemaker **MonitoringSchedule** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
sagemaker controller. The chart renders a native `sagemaker.services.k8s.aws/v1alpha1` `MonitoringSchedule`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsSagemakerMonitoringschedule` Composition you create
becomes an AWS MonitoringSchedule.

## Prerequisites

- **ACK sagemaker controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-sagemaker-controller \
    oci://public.ecr.aws/aws-controllers-k8s/sagemaker-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK MonitoringSchedule spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-sagemaker-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsSagemakerMonitoringschedule type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsSagemakerMonitoringschedule` Composition type (`composition.krateo.io/v0-1-0`,
plural `awssagemakermonitoringschedules`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-sagemaker-monitoringschedule`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsSagemakerMonitoringschedule
metadata:
  name: my-monitoringschedule
  namespace: aws-sagemaker-system
spec:
  region: eu-west-1
  # ... ACK MonitoringSchedule spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awssagemakermonitoringschedules.composition.krateo.io -n aws-sagemaker-system
kubectl get monitoringschedules.sagemaker.services.k8s.aws -A
```
