# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sagemaker-pipeline
  namespace: aws-sagemaker-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: MachineLearning
spec:
  apiRef:
    name: aws-sagemaker-pipeline-card-data
    namespace: aws-sagemaker-system
  widgetData:
    title: Aws Sagemaker Pipeline
    headerLeft: Provision an AWS Sagemaker Pipeline via the ACK sagemaker controller.
    tooltip: Aws Sagemaker Pipeline blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - MachineLearning
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sagemaker-pipeline-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["MachineLearning", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sagemaker-pipeline-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sagemaker-pipeline-install
        namespace: aws-sagemaker-system
        resource: buttons
        verb: GET
