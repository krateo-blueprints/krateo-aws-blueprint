# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-secretsmanager-secret
  namespace: aws-secretsmanager-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Security
spec:
  apiRef:
    name: aws-secretsmanager-secret-card-data
    namespace: aws-secretsmanager-system
  widgetData:
    title: Aws Secretsmanager Secret
    headerLeft: Provision an AWS Secretsmanager Secret via the ACK secretsmanager controller.
    tooltip: Aws Secretsmanager Secret blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Security
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-secretsmanager-secret-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Security", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-secretsmanager-secret-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-secretsmanager-secret-install
        namespace: aws-secretsmanager-system
        resource: buttons
        verb: GET
