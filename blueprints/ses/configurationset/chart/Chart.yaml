apiVersion: v2
# Generated by tools/ackgen from the ACK ConfigurationSet CRD (ses.services.k8s.aws).
#
# Renders a native ses.services.k8s.aws/v1alpha1 ConfigurationSet resource; the ACK ses
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsSesConfigurationset (plural awssesconfigurationsets) in the composition.krateo.io group.
# The Composition spec mirrors the ACK ConfigurationSet spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-ses-configurationset
description: A Krateo blueprint that provisions an AWS Ses ConfigurationSet via the AWS Controllers for Kubernetes (ACK) ses controller. Requires the ACK ses controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - ses
  - configurationset
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
