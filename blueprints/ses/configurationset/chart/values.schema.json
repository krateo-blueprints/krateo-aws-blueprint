{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Provision an AWS Ses ConfigurationSet via the ACK ses controller. Fields mirror the ses.services.k8s.aws/v1alpha1 ConfigurationSet spec.",
  "properties": {
    "name": {
      "type": "string"
    },
    "region": {
      "default": "",
      "description": "AWS region for this resource (e.g. eu-west-1). Rendered as the services.k8s.aws/region annotation. Empty inherits the ACK controller's default region.",
      "title": "AWS region",
      "type": "string"
    }
  },
  "title": "AWS Ses ConfigurationSet",
  "type": "object"
}
