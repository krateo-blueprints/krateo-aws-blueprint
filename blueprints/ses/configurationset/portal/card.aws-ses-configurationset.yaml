# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ses-configurationset
  namespace: aws-ses-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-ses-configurationset-card-data
    namespace: aws-ses-system
  widgetData:
    title: Aws Ses Configurationset
    headerLeft: Provision an AWS Ses ConfigurationSet via the ACK ses controller.
    tooltip: Aws Ses Configurationset blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ses-configurationset-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ses-configurationset-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ses-configurationset-install
        namespace: aws-ses-system
        resource: buttons
        verb: GET
