# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sfn-activity
  namespace: aws-sfn-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sfn-activity-card-data
    namespace: aws-sfn-system
  widgetData:
    title: Aws Sfn Activity
    headerLeft: Provision an AWS Sfn Activity via the ACK sfn controller.
    tooltip: Aws Sfn Activity blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sfn-activity-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sfn-activity-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sfn-activity-install
        namespace: aws-sfn-system
        resource: buttons
        verb: GET
