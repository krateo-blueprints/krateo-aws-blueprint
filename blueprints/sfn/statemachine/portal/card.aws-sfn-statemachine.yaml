# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sfn-statemachine
  namespace: aws-sfn-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-sfn-statemachine-card-data
    namespace: aws-sfn-system
  widgetData:
    title: Aws Sfn Statemachine
    headerLeft: Provision an AWS Sfn StateMachine via the ACK sfn controller.
    tooltip: Aws Sfn Statemachine blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sfn-statemachine-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sfn-statemachine-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sfn-statemachine-install
        namespace: aws-sfn-system
        resource: buttons
        verb: GET
