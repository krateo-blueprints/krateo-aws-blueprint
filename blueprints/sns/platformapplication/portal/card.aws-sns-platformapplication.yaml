# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sns-platformapplication
  namespace: aws-sns-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sns-platformapplication-card-data
    namespace: aws-sns-system
  widgetData:
    title: Aws Sns Platformapplication
    headerLeft: Provision an AWS Sns PlatformApplication via the ACK sns controller.
    tooltip: Aws Sns Platformapplication blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sns-platformapplication-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sns-platformapplication-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sns-platformapplication-install
        namespace: aws-sns-system
        resource: buttons
        verb: GET
