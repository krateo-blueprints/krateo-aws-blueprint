# Krateo Blueprint — AWS Sns PlatformEndpoint

> Generated by `tools/ackgen` from the ACK `PlatformEndpoint` CRD (`sns.services.k8s.aws`).

Provisions an AWS Sns **PlatformEndpoint** through the
[AWS Controllers for Kubernetes (ACK)](https://aws-controllers-k8s.github.io/community/)
sns controller. The chart renders a native `sns.services.k8s.aws/v1alpha1` `PlatformEndpoint`
resource; the ACK controller reconciles it into the real AWS resource and reports status back.

Once the `CompositionDefinition` is applied, every `AwsSnsPlatformendpoint` Composition you create
becomes an AWS PlatformEndpoint.

## Prerequisites

- **ACK sns controller installed** — see
  [`../../../docs/installing-controllers.md`](../../../docs/installing-controllers.md):
  ```sh
  helm install ack-sns-controller \
    oci://public.ecr.aws/aws-controllers-k8s/sns-chart \
    --namespace ack-system --create-namespace \
    --set aws.region=eu-west-1
  ```
- **AWS credentials configured** on that controller (IRSA/Pod Identity or static) — see
  [`../../../docs/authentication.md`](../../../docs/authentication.md).
- Krateo `core-provider` installed.

## Configuration

Composition `spec` fields **mirror the ACK PlatformEndpoint spec**. Full schema in
[`chart/values.schema.json`](chart/values.schema.json). The extra `region` field selects the
AWS region (empty = controller default).

## How to install

```sh
kubectl create namespace aws-sns-system
kubectl apply -f compositiondefinition.yaml   # publishes the AwsSnsPlatformendpoint type
kubectl apply -f customform.yaml              # optional: portal card + form
```

This publishes a `AwsSnsPlatformendpoint` Composition type (`composition.krateo.io/v0-1-0`,
plural `awssnsplatformendpoints`), pulling the chart from
`oci://ghcr.io/braghettos/charts/aws-sns-platformendpoint`.

### Create a Composition

```yaml
apiVersion: composition.krateo.io/v0-1-0
kind: AwsSnsPlatformendpoint
metadata:
  name: my-platformendpoint
  namespace: aws-sns-system
spec:
  region: eu-west-1
  # ... ACK PlatformEndpoint spec fields (see values.schema.json)
```

### Check status

```sh
kubectl get awssnsplatformendpoints.composition.krateo.io -n aws-sns-system
kubectl get platformendpoints.sns.services.k8s.aws -A
```
