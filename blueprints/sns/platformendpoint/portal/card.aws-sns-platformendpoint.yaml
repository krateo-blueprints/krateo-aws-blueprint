# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sns-platformendpoint
  namespace: aws-sns-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sns-platformendpoint-card-data
    namespace: aws-sns-system
  widgetData:
    title: Aws Sns Platformendpoint
    headerLeft: Provision an AWS Sns PlatformEndpoint via the ACK sns controller.
    tooltip: Aws Sns Platformendpoint blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sns-platformendpoint-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sns-platformendpoint-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sns-platformendpoint-install
        namespace: aws-sns-system
        resource: buttons
        verb: GET
