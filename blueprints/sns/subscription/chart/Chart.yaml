apiVersion: v2
# Generated by tools/ackgen from the ACK Subscription CRD (sns.services.k8s.aws).
#
# Renders a native sns.services.k8s.aws/v1alpha1 Subscription resource; the ACK sns
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsSnsSubscription (plural awssnssubscriptions) in the composition.krateo.io group.
# The Composition spec mirrors the ACK Subscription spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-sns-subscription
description: A Krateo blueprint that provisions an AWS Sns Subscription via the AWS Controllers for Kubernetes (ACK) sns controller. Requires the ACK sns controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - sns
  - subscription
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "application"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
