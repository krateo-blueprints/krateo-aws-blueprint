# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sns-subscription
  namespace: aws-sns-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-sns-subscription-card-data
    namespace: aws-sns-system
  widgetData:
    title: Aws Sns Subscription
    headerLeft: Provision an AWS Sns Subscription via the ACK sns controller.
    tooltip: Aws Sns Subscription blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sns-subscription-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sns-subscription-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sns-subscription-install
        namespace: aws-sns-system
        resource: buttons
        verb: GET
