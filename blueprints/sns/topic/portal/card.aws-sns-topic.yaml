# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sns-topic
  namespace: aws-sns-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-sns-topic-card-data
    namespace: aws-sns-system
  widgetData:
    title: Aws Sns Topic
    headerLeft: Provision an AWS Sns Topic via the ACK sns controller.
    tooltip: Aws Sns Topic blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sns-topic-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sns-topic-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sns-topic-install
        namespace: aws-sns-system
        resource: buttons
        verb: GET
