# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-sqs-queue
  namespace: aws-sqs-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Integration
spec:
  apiRef:
    name: aws-sqs-queue-card-data
    namespace: aws-sqs-system
  widgetData:
    title: Aws Sqs Queue
    headerLeft: Provision an AWS Sqs Queue via the ACK sqs controller.
    tooltip: Aws Sqs Queue blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Integration
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-sqs-queue-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Integration", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-sqs-queue-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-sqs-queue-install
        namespace: aws-sqs-system
        resource: buttons
        verb: GET
