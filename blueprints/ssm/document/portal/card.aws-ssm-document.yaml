# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ssm-document
  namespace: aws-ssm-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-ssm-document-card-data
    namespace: aws-ssm-system
  widgetData:
    title: Aws Ssm Document
    headerLeft: Provision an AWS Ssm Document via the ACK ssm controller.
    tooltip: Aws Ssm Document blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ssm-document-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ssm-document-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ssm-document-install
        namespace: aws-ssm-system
        resource: buttons
        verb: GET
