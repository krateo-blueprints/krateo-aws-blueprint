# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ssm-parameter
  namespace: aws-ssm-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-ssm-parameter-card-data
    namespace: aws-ssm-system
  widgetData:
    title: Aws Ssm Parameter
    headerLeft: Provision an AWS Ssm Parameter via the ACK ssm controller.
    tooltip: Aws Ssm Parameter blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ssm-parameter-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ssm-parameter-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ssm-parameter-install
        namespace: aws-ssm-system
        resource: buttons
        verb: GET
