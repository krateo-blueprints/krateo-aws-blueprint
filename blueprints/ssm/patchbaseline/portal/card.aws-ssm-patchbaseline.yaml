# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ssm-patchbaseline
  namespace: aws-ssm-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Management
spec:
  apiRef:
    name: aws-ssm-patchbaseline-card-data
    namespace: aws-ssm-system
  widgetData:
    title: Aws Ssm Patchbaseline
    headerLeft: Provision an AWS Ssm PatchBaseline via the ACK ssm controller.
    tooltip: Aws Ssm Patchbaseline blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - Management
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ssm-patchbaseline-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Management", .version] }
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ssm-patchbaseline-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ssm-patchbaseline-install
        namespace: aws-ssm-system
        resource: buttons
        verb: GET
