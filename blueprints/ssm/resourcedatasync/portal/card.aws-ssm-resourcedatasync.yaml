# GENERATED by gen-portal-widgets.cjs from ../customform.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: aws-ssm-resourcedatasync
  namespace: aws-ssm-system
  labels:
    krateo.io/portal-page: blueprints
spec:
  apiRef:
    name: aws-ssm-resourcedatasync-card-data
    namespace: aws-ssm-system
  widgetData:
    title: Aws Ssm Resourcedatasync
    headerLeft: Provision an AWS Ssm ResourceDataSync via the ACK ssm controller.
    tooltip: Aws Ssm Resourcedatasync blueprint
    icon:
      name: fa-aws
      color: grey
    tags:
      - aws
      - cloud
      - iaas
    extra: ''
    items: []
    footer:
      - resourceRefId: aws-ssm-resourcedatasync-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready + (if .version != "—" then " · v" + .version else "" end) }'
    - forPath: icon
      expression: '${ { name: "fa-aws", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: aws-ssm-resourcedatasync-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: aws-ssm-resourcedatasync-install
        namespace: aws-ssm-system
        resource: buttons
        verb: GET
