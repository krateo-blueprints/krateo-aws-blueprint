apiVersion: v2
# Generated by tools/ackgen from the ACK RuleGroup CRD (wafv2.services.k8s.aws).
#
# Renders a native wafv2.services.k8s.aws/v1alpha1 RuleGroup resource; the ACK wafv2
# controller reconciles it into the real AWS resource.
#
# Composition Kind: AwsWafv2Rulegroup (plural awswafv2rulegroups) in the composition.krateo.io group.
# The Composition spec mirrors the ACK RuleGroup spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: aws-wafv2-rulegroup
description: A Krateo blueprint that provisions an AWS Wafv2 RuleGroup via the AWS Controllers for Kubernetes (ACK) wafv2 controller. Requires the ACK wafv2 controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "v1alpha1"

home: https://krateo.io
icon: "https://raw.githubusercontent.com/krateoplatformops/krateo/main/docs/media/logo.svg"
keywords:
  - aws
  - wafv2
  - rulegroup
  - ack
sources:
  - https://github.com/braghettos/krateo-aws-blueprint
annotations:
  krateo.io/category: "security"
  krateo.io/maturity: "beta"
  krateoSupportedVersion: "2.7.0"
