apiVersion: v2
# Generated by tools/ackgen from the ACK [[.AckKind]] CRD ([[.Group]]).
#
# Renders a native [[.Group]]/[[.AckVersion]] [[.AckKind]] resource; the ACK [[.Service]]
# controller reconciles it into the real AWS resource.
#
# Composition Kind: [[.CompKind]] (plural [[.CompPlural]]) in the composition.krateo.io group.
# The Composition spec mirrors the ACK [[.AckKind]] spec; core-provider builds the Composition
# CRD from values.schema.json only.
name: [[.ChartName]]
description: A Krateo blueprint that provisions an AWS [[.ServiceTitle]] [[.AckKind]] via the AWS Controllers for Kubernetes (ACK) [[.Service]] controller. Requires the ACK [[.Service]] controller and AWS credentials to be installed/configured beforehand (see repo docs).
type: application

# Drives the Composition CRD version (0.1.0 -> v0-1-0). Stamped by CI.
version: CHART_VERSION
# ACK CRD version this blueprint targets.
appVersion: "[[.AckVersion]]"

home: https://krateo.io
icon: "https://github.com/krateoplatformops/krateo/blob/main/docs/media/logo.svg"
keywords:
  - aws
  - [[.Service]]
  - [[.AckKindLower]]
  - ack
sources:
  - https://github.com/krateo-blueprints/krateo-aws-blueprint
annotations:
  krateoSupportedVersion: "2.7.0"
